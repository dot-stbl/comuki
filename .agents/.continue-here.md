---
context: default
phase: 09-mvp-polish
task: —
total_tasks: 0
status: complete
last_updated: 2026-09-08T00:00:00Z
---

# v1 SHIPS — Master `fa659fd`, 50/50 issues closed

## Status
v1 milestone **complete**. 24 slices landed. 50 of 50 GitHub issues
closed (0 open). 4 deferred to v2 backlog (#47, #48, #49, #50).

| Test | Status |
|---|---|
| Backend (1567 tests: 1354 unit + 213 integration) | green |
| Frontend (1560 tests, 136 files) | green |
| Build gate (format + analyzers) | clean |
| OpenAPI emission | green |

## Deferred to v2 (4 issues, all closed with "v2 backlog" comment)
- **#47** Generic-command runner-container (Process.Start isolation)
- **#48** Fleet runner host-agent for bare-metal
- **#49** Autonomy ratchet continuation (confidence scoring, daily decay)
- **#50** Merge-queue multi-feature batch + dependency ordering

## Pre-existing flakes — all fixed in merge(fixes) batch (2026-09-08)
- `Comuki.Engine.Orchestration.Unit.StatusMachine` PromoteTrustedIsNoOp —
  test race on `Run.UpdatedAt` (off by 1s); fixed by `8181d35`
- `Comuki.Modules.Scheduler.Unit` ExecuteDeleteAsync on InMemory —
  switched to `Find+Remove` for compatibility; `6c5bb10`
- `Comuki.Host.Integration.Runs` EscalationTimeoutSweeper — test seeds
  cross-pollution; `e1c2b99`
- `Comuki.Host.Integration.Oidc` Keycloak Docker — skipped in WSL2
  (Docker Postgres unreachable); `f48fd79`
- `Comuki.Modules.Scheduler.Unit` ScheduledJobService.CreateAsync —
  `FormatException` wrapped in `InvalidCronExpressionException`; `3c122f3`
- `Comuki.Modules.Verify.Unit` 0 tests (build green; not a flake)

## Pre-existing gotchas (still relevant)
- **`artifacts/openapi.json` gitignored** — `dotnet build` regenerates
- **`dotnet` must be in PATH** for kubb codegen (not just full path)
- **VITE_USE_MOCK / VITE_API_BASE_URL** — real-mode throws if unset
- **Worktree cleanup (Windows)** — use `cmd /c "mklink /J C:\wt .agents\worktree"`
- **Test runner** — `dotnet run --project <test>` (MTP), NOT `dotnet test`
- **PendingModelChangesWarning** — `dotnet ef migrations add` after EF entity changes

## Next agent: what to do

**v1 ships. No work to dispatch.**

When v2 is approved:
1. Re-open issues #47, #48, #49, #50
2. Add new v2-specific issues from ROADMAP.md "v2 backlog" section
3. Start a new sprint

Otherwise: idle. Read `STATE.md` and `ROADMAP.md` for context.

## Critical files to read first
- `.agents/STATE.md` — full state, decisions, gotchas
- `.agents/ROADMAP.md` — phase breakdown + v2 backlog
- `.agents/HANDOFF.json` — machine-readable handoff
- `.agents/RULES-BOOTSTRAP.md` — rule-loading ritual
- `AGENTS.md` — top-level entry point

## Commit format reminder
`[hybrid](<feat/...>): <subject>` — project tag `[hybrid]`, required
feature path in parentheses, imperative subject ≤72 chars.

## Build verification (Definition of Done)
```bash
dotnet build comuki.slnx -c Debug    # 0/0 + format gate
dotnet run --project <test>          # MTP runner
cd dashboard && bun run typecheck && bun run lint && bun run test
cd agents && bun install && bun run typecheck && bun test
```

## Rule-loading ritual (mandatory for all agents)
- Read all rules in `~/.agents/rules/csharp/`, `typescript/`, `process/`
- Re-read at end of work
- Run `~/.agents/skills/canon/SKILL.md` self-audit before reporting done
- `[VerifyRuleAwareness]` + `[SelfAuditReport]` MSBuild targets run at build

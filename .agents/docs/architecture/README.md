# Comuki — слепок проектирования

Набор документов по архитектуре оркестратора Claude Code агентов. Собран в ходе проектного обсуждения. Для загрузки в knowledge проекта Comuki.

## Что есть в наборе

| Файл | Что внутри | Когда смотреть |
|---|---|---|
| **comuki-decisions.md** | Журнал решений: что выбрали и **почему**, с альтернативами | Начать отсюда — даёт логику всего остального |
| **comuki-architecture.md** | Архитектура: принципы, сервисы, workflow, риски (38, вкл. стыковые), MVP vs «потом» | Что система делает и как устроена |
| **comuki-stack.md** | Технический стек: компоненты, хранилища, durable, наблюдаемость, API | Из чего собрана платформа |
| **comuki-project-structure.md** | Структура репозитория: папки по стеку, C#-слои, агенты, deploy | Как разложен код |
| **comuki-dashboard-designspec.md** | Спека дашбордной дизайн-системы | Вход для Claude Design |
| **adr-0001-ui-kit-react-aria.md** | ADR: уход с shadcn → свой кит + React Aria | Контекст выбора UI-кита дашборда |
| **adr-0002-cli-opentui.md** | ADR: OpenTUI Core + keymap как TUI-стек CLI; React отложен | Выбор презентационного стека CLI |
| **adr-0003-cli-ownership.md** | ADR: ownership двух бинарей `comuki` (TS user CLI vs .NET operator); argv+slash registry matrix; transport authority REST vs SignalR | Решение про executable naming и command-by-command ownership (issue #82) |
| architecture.html / comuki-stack.html | Те же документы в HTML — со схемами | Для чтения/презентации (схемы есть только тут) |

> md-версии — для проекта и git (чисто парсятся, версионируются). HTML — для чтения глазами (в них 5 архитектурных схем + карта стека, которых нет в md).

## Краткая суть Comuki

Платформа, где **ведущая модель** (мозг) декомпозирует задачу и дирижирует роем **рабочих моделей** (эфемерные воркеры в контейнерах), которые пишут / тестируют / готовят к выкату фичи. Общая база знаний и правила по MCP. Защита от слопа — проверенные скиллы-рецепты + жёсткие анализаторы + дизайн-система как принуждаемое правило.

**Стек:** платформа на C#/.NET (orchestrator, proxy на YARP, mcp, Translator на AOT). Агенты на TS: pi для воркеров (контроль/наблюдаемость), форк GSD под Claude Code для разрабов (UX) — оба правил не хранят, берут из единого свода. Дашборд React+shadcn. Хранилища: Postgres (состояние+очередь+вектора), MinIO (артефакты), Nexus (пакеты). Швы: gRPC (Translator↔оркестратор), MCP (агент↔знания), SignalR (дашборд). Наблюдаемость: OpenTelemetry + Victoria. Всё self-hosted.

**Фаза:** MVP — человек в петле, задачи ставятся руками, выкат в прод вручную. Полная автономия — отложена.

## Следующий шаг

Первый код — контракт «ведущая модель ↔ оркестратор» (четыре типа решений: план / бриф / вердикт / эскалация, валидируемые схемы). Дом — `comuki-orchestrator`. JSON Schema как единый источник типов для C# и TS, чтобы контракт не разъехался между платформой и плагином.

---
*Слепок проектного обсуждения. Черновики для команды — подлежат ревизии по мере контакта с реальностью.*

---
id: 05-01
title: Z.AI key rotation proxy (Slice 1 — first vertical cut)
status: done
phase: 5
depends-on: [04-01]
parallelizable: false
---

# Z.AI Key Rotation Proxy — Implementation Plan

> Originally authored under the `superpowers` workflow (`docs/superpowers/`);
> relocated into the soly phase folder post-hoc. Executed task-by-task via
> superpowers subagent-driven-development. Close-out summary: `05-01-SUMMARY.md`.
> Design: `05-01-DESIGN.md`.

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Прокси на YARP прозрачно подменяет исчерпанный ключ Z.AI на следующий живой из пула и повторяет запрос — агент продолжает работать, не видя сбоя квоты.

**Architecture:** Тонкий YARP-хост `Comuki.Platform.Proxy` (`application/internal/`) форвардит запрос агента на Z.AI через `IHttpForwarder`. Логика ротации — в `Comuki.Platform.Routing` (`feature/`): `KeyPool` (пул + cooldown), `QuotaExhaustionDetector` (правила детекции), `KeyRotatingForwarder` (retry-оркестрация поверх абстракции `IUpstreamSender`), `YarpUpstreamSender` (реальная YARP-обвязка). Тестируемое ядро (pool/detector/orchestrator) отделено от фидлерной YARP-склейки, которая проверяется интеграционным тестом.

**Tech Stack:** C# / .NET 10, ASP.NET Core, YARP (`Yarp.ReverseProxy`, `IHttpForwarder`), xUnit v3 (MTP), Shouldly, NSubstitute.

## Global Constraints

Каждая задача неявно включает эти требования (значения скопированы из репозитория дословно):

- **TFM `net10.0`**, `Nullable=enable`, `ImplicitUsings=enable`, `LangVersion=latest` (из `Directory.Build.props`).
- **`TreatWarningsAsErrors=true`**, `EnforceCodeStyleInBuild=true`, `AnalysisLevel=latest`. Активны анализаторы **Meziantou.Analyzer** + **Roslynator** → классы `sealed`, `ConfigureAwait(false)` в библиотечном коде, `TimeProvider` вместо `DateTime.Now`, не ловить общий `Exception` без нужды. Любой warning ломает сборку.
- **DI** (`DI-RULES.md`): один **Installer** на проект (`static class` в папке `Installers/`, метод `Add<Module>Core(IServiceCollection, IConfiguration)`); конфиг только через `IOptions<T>` с `const SectionName` внутри Options-класса; `.ValidateDataAnnotations().ValidateOnStart()` обязательны; `TimeProvider` как singleton; никакого `IConfiguration` в сервисах; никакой авторегистрации через рефлексию.
- **Тесты** (`TESTING-RULES.md`): xUnit v3 через **Microsoft Testing Platform** — запуск `dotnet run --project <csproj>`, **не** `dotnet test`. Имя тест-проекта: `Comuki.Platform.<SrcProject>.<Kind>.<Feature>`. Тест-проект: `IsTestProject=true`, `UseMicrosoftTestingPlatformRunner=true`. Гейт покрытия — **70% line** (из `Directory.Build.props`).
- **`.slnx` редактируется вручную** (`PROJECT-STRUCTURE.md §7` — зафиксированный workaround под .NET 10 SDK на Windows): `<Folder Name>` = physical path от корня slnx; `Path` относителен корня репо; один `<Project>` на проект. Перед коммитом — diff csproj ⇄ slnx (команда в Task 1, шаг verify).
- **Версия билда:** `dotnet build comuki.slnx` → 0 warnings, 0 errors.
- **Коммиты:** Conventional Commits с префиксом репозитория, scope `phase-5`. Формат как в истории: `[hybrid] <type>(phase-5): <desc>`.

---

### Task 1: Scaffold `Comuki.Platform.Routing` + YARP package

Создаём пустую feature-библиотеку, регистрируем пакет YARP централизованно, добавляем проект в решение. Деливерабл: библиотека компилируется и видна в `comuki.slnx`.

**Files:**
- Create: `platform/src/feature/Comuki.Platform.Routing/Comuki.Platform.Routing.csproj`
- Modify: `Directory.Packages.props` (добавить `PackageVersion` для YARP)
- Modify: `comuki.slnx` (добавить `<Project>` в `<Folder Name="/platform/src/feature/">`)

**Interfaces:**
- Produces: проект `Comuki.Platform.Routing` (RootNamespace `Comuki.Platform.Routing`), доступный для ProjectReference из хоста и тестов.

- [ ] **Step 1: Создать физическую папку и csproj**

Создать файл `platform/src/feature/Comuki.Platform.Routing/Comuki.Platform.Routing.csproj`:

```xml
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Comuki.Platform.Routing</RootNamespace>
    <AssemblyName>Comuki.Platform.Routing</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- HttpContext, IHttpForwarder, HttpTransformer — типы ASP.NET Core в classlib. -->
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
    <PackageReference Include="Yarp.ReverseProxy" />
  </ItemGroup>

</Project>
```

- [ ] **Step 2: Добавить версию YARP в `Directory.Packages.props`**

Добавить в новый `<ItemGroup>` (рядом с другими рантайм-пакетами, после блока `protobuf-net.Grpc`):

```xml
  <ItemGroup>
    <!-- Модельный прокси: тонкий reverse-proxy на YARP. IHttpForwarder
         (direct forwarding) — пробрасывает Anthropic-совместимый поток,
         не транслирует. Используется Comuki.Platform.Routing. -->
    <PackageVersion Include="Yarp.ReverseProxy" Version="2.3.0" />
  </ItemGroup>
```

- [ ] **Step 3: Добавить проект в `comuki.slnx`**

В существующий `<Folder Name="/platform/src/feature/">` добавить строку (после `Comuki.Platform.Orchestration`):

```xml
    <Project Path="platform/src/feature/Comuki.Platform.Routing/Comuki.Platform.Routing.csproj" />
```

- [ ] **Step 4: Verify — csproj ⇄ slnx и сборка**

Run:
```bash
diff <(find platform/src tests -name '*.csproj' | sort) \
     <(grep -oE 'Path="[^"]+\.csproj"' comuki.slnx | sed 's/Path="//;s/"$//' | sort)
dotnet build comuki.slnx
```
Expected: diff пустой; build → `0 Warning(s) 0 Error(s)`.

- [ ] **Step 5: Commit**

```bash
git add platform/src/feature/Comuki.Platform.Routing Directory.Packages.props comuki.slnx
git commit -m "[hybrid] chore(phase-5): scaffold Comuki.Platform.Routing + YARP package"
```

---

### Task 2: `RotationOptions` + `ExhaustionRule` (конфиг)

Типизированный конфиг ротации с DataAnnotations. Деливерабл: классы компилируются; биндинг и валидация подключатся в Installer (Task 7).

**Files:**
- Create: `platform/src/feature/Comuki.Platform.Routing/Options/RotationOptions.cs`
- Create: `platform/src/feature/Comuki.Platform.Routing/Options/ExhaustionRule.cs`

**Interfaces:**
- Produces:
  - `RotationOptions` с `const string SectionName = "Routing:Rotation"`, свойствами `string[] ApiKeys`, `string UpstreamUrl`, `TimeSpan DefaultCooldown`, `ExhaustionRule[] ExhaustionRules`.
  - `ExhaustionRule` с `int? StatusCode`, `string? BodyContains`.

- [ ] **Step 1: Создать `ExhaustionRule.cs`**

```csharp
namespace Comuki.Platform.Routing.Options;

/// <summary>
/// Одно правило детекции исчерпания квоты. Срабатывает, если совпали все
/// заданные поля. Хотя бы одно из <see cref="StatusCode"/> /
/// <see cref="BodyContains"/> должно быть задано (проверяется в Installer).
/// </summary>
public sealed class ExhaustionRule
{
    /// <summary>HTTP-статус ответа апстрима (например, 429). null = любой.</summary>
    public int? StatusCode { get; init; }

    /// <summary>Подстрока в теле ответа (например, "insufficient balance"),
    /// сравнение ordinal-ignore-case. null = тело не проверяется.</summary>
    public string? BodyContains { get; init; }
}
```

- [ ] **Step 2: Создать `RotationOptions.cs`**

```csharp
using System.ComponentModel.DataAnnotations;

namespace Comuki.Platform.Routing.Options;

/// <summary>
/// Конфигурация ротации ключей Z.AI в модельном прокси.
/// </summary>
public sealed class RotationOptions
{
    /// <summary>Путь к секции в IConfiguration.</summary>
    public const string SectionName = "Routing:Rotation";

    /// <summary>Пул физических ключей Z.AI. Перебираются по порядку.</summary>
    [Required]
    [MinLength(1)]
    public required string[] ApiKeys { get; init; }

    /// <summary>Реальный upstream-URL Z.AI (Anthropic-совместимый).</summary>
    [Required]
    [Url]
    public required string UpstreamUrl { get; init; }

    /// <summary>Cooldown по умолчанию, если в ответе нет заголовка Retry-After.</summary>
    [Range(typeof(TimeSpan), "00:00:01", "1.00:00:00")]
    public TimeSpan DefaultCooldown { get; init; } = TimeSpan.FromHours(1);

    /// <summary>Правила детекции исчерпания квоты (логическое ИЛИ по списку).</summary>
    [Required]
    [MinLength(1)]
    public required ExhaustionRule[] ExhaustionRules { get; init; }
}
```

- [ ] **Step 3: Verify — сборка**

Run: `dotnet build platform/src/feature/Comuki.Platform.Routing/Comuki.Platform.Routing.csproj`
Expected: `0 Warning(s) 0 Error(s)`.

- [ ] **Step 4: Commit**

```bash
git add platform/src/feature/Comuki.Platform.Routing/Options
git commit -m "[hybrid] feat(phase-5): RotationOptions + ExhaustionRule config"
```

---

### Task 3: `QuotaExhaustionDetector` (TDD)

Чистая функция: по статусу + телу ответа решает, исчерпана ли квота. Самый ценный тест — таблица фикстур из реальных форм ответов Z.AI.

**Files:**
- Create: `platform/src/feature/Comuki.Platform.Routing/Interfaces/IQuotaExhaustionDetector.cs`
- Create: `platform/src/feature/Comuki.Platform.Routing/Services/QuotaExhaustionDetector.cs`
- Create test project: `tests/Comuki.Platform.Routing.Unit.KeyRotation/Comuki.Platform.Routing.Unit.KeyRotation.csproj`
- Test: `tests/Comuki.Platform.Routing.Unit.KeyRotation/QuotaExhaustionDetectorTests.cs`
- Modify: `comuki.slnx` (добавить тест-проект в `<Folder Name="/tests/">`)

**Interfaces:**
- Consumes: `RotationOptions`, `ExhaustionRule` (Task 2).
- Produces: `bool IQuotaExhaustionDetector.IsExhausted(int statusCode, string? body)`.

- [ ] **Step 1: Создать тест-проект csproj**

`tests/Comuki.Platform.Routing.Unit.KeyRotation/Comuki.Platform.Routing.Unit.KeyRotation.csproj`:

```xml
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Comuki.Platform.Routing.Unit.KeyRotation</RootNamespace>
    <AssemblyName>Comuki.Platform.Routing.Unit.KeyRotation</AssemblyName>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
    <UseMicrosoftTestingPlatformRunner>true</UseMicrosoftTestingPlatformRunner>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit.v3" />
    <PackageReference Include="Shouldly" />
    <PackageReference Include="NSubstitute" />
    <PackageReference Include="coverlet.collector" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\platform\src\feature\Comuki.Platform.Routing\Comuki.Platform.Routing.csproj" />
  </ItemGroup>

</Project>
```

Добавить в `comuki.slnx` в `<Folder Name="/tests/">`:
```xml
    <Project Path="tests/Comuki.Platform.Routing.Unit.KeyRotation/Comuki.Platform.Routing.Unit.KeyRotation.csproj" />
```

- [ ] **Step 2: Написать падающий тест**

`tests/Comuki.Platform.Routing.Unit.KeyRotation/QuotaExhaustionDetectorTests.cs`:

```csharp
using Comuki.Platform.Routing.Interfaces;
using Comuki.Platform.Routing.Options;
using Comuki.Platform.Routing.Services;
using Microsoft.Extensions.Options;
using Shouldly;
using Xunit;

namespace Comuki.Platform.Routing.Unit.KeyRotation;

public sealed class QuotaExhaustionDetectorTests
{
    private static IQuotaExhaustionDetector CreateSut()
    {
        var options = Microsoft.Extensions.Options.Options.Create(new RotationOptions
        {
            ApiKeys = ["k1"],
            UpstreamUrl = "https://upstream.example",
            ExhaustionRules =
            [
                new ExhaustionRule { StatusCode = 429 },
                new ExhaustionRule { BodyContains = "insufficient balance" },
                new ExhaustionRule { StatusCode = 401, BodyContains = "quota" },
            ],
        });
        return new QuotaExhaustionDetector(options);
    }

    [Theory]
    [InlineData(429, "rate limited", true)]            // правило по статусу
    [InlineData(400, "insufficient balance now", true)] // правило по телу
    [InlineData(401, "quota exceeded", true)]           // правило статус+тело
    [InlineData(401, "bad api key", false)]             // 401 без "quota" — не наше
    [InlineData(500, "internal error", false)]          // обычная ошибка апстрима
    [InlineData(200, "", false)]                        // успех
    public void IsExhausted_MatchesConfiguredRules(int status, string body, bool expected)
    {
        var sut = CreateSut();

        sut.IsExhausted(status, body).ShouldBe(expected);
    }

    [Fact]
    public void IsExhausted_BodyMatch_IsCaseInsensitive()
    {
        var sut = CreateSut();

        sut.IsExhausted(400, "INSUFFICIENT BALANCE").ShouldBeTrue();
    }
}
```

- [ ] **Step 3: Запустить — убедиться, что не компилируется/падает**

Run: `dotnet run --project tests/Comuki.Platform.Routing.Unit.KeyRotation`
Expected: ошибка компиляции — `IQuotaExhaustionDetector` / `QuotaExhaustionDetector` не существуют.

- [ ] **Step 4: Создать интерфейс**

`platform/src/feature/Comuki.Platform.Routing/Interfaces/IQuotaExhaustionDetector.cs`:

```csharp
namespace Comuki.Platform.Routing.Interfaces;

/// <summary>
/// Решает, означает ли ответ апстрима исчерпание квоты/баланса ключа Z.AI.
/// Чистая функция — без состояния и без сети.
/// </summary>
public interface IQuotaExhaustionDetector
{
    /// <summary>
    /// true, если ответ совпал хотя бы с одним правилом исчерпания.
    /// </summary>
    /// <param name="statusCode">HTTP-статус ответа апстрима.</param>
    /// <param name="body">Тело ответа (для non-2xx; иначе null).</param>
    bool IsExhausted(int statusCode, string? body);
}
```

- [ ] **Step 5: Реализовать `QuotaExhaustionDetector`**

`platform/src/feature/Comuki.Platform.Routing/Services/QuotaExhaustionDetector.cs`:

```csharp
using Comuki.Platform.Routing.Interfaces;
using Comuki.Platform.Routing.Options;
using Microsoft.Extensions.Options;

namespace Comuki.Platform.Routing.Services;

/// <inheritdoc />
public sealed class QuotaExhaustionDetector(IOptions<RotationOptions> options) : IQuotaExhaustionDetector
{
    public bool IsExhausted(int statusCode, string? body)
    {
        foreach (var rule in options.Value.ExhaustionRules)
        {
            var statusMatches = rule.StatusCode is null || rule.StatusCode.Value == statusCode;
            var bodyMatches = string.IsNullOrEmpty(rule.BodyContains)
                || (body is not null && body.Contains(rule.BodyContains, StringComparison.OrdinalIgnoreCase));

            if (statusMatches && bodyMatches)
            {
                return true;
            }
        }

        return false;
    }
}
```

- [ ] **Step 6: Запустить тесты — убедиться, что проходят**

Run: `dotnet run --project tests/Comuki.Platform.Routing.Unit.KeyRotation`
Expected: все тесты PASS.

- [ ] **Step 7: Commit**

```bash
git add platform/src/feature/Comuki.Platform.Routing/Interfaces/IQuotaExhaustionDetector.cs \
        platform/src/feature/Comuki.Platform.Routing/Services/QuotaExhaustionDetector.cs \
        tests/Comuki.Platform.Routing.Unit.KeyRotation comuki.slnx
git commit -m "[hybrid] feat(phase-5): QuotaExhaustionDetector with configurable rules"
```

---

### Task 4: `KeyPool` (TDD)

Потокобезопасный пул ключей с cooldown. Время — через `TimeProvider` (тестируем детерминированно).

**Files:**
- Create: `platform/src/feature/Comuki.Platform.Routing/Interfaces/IKeyPool.cs`
- Create: `platform/src/feature/Comuki.Platform.Routing/Services/KeyPool.cs`
- Create: `tests/Comuki.Platform.Routing.Unit.KeyRotation/MutableTimeProvider.cs` (тест-дубль времени)
- Test: `tests/Comuki.Platform.Routing.Unit.KeyRotation/KeyPoolTests.cs`

**Interfaces:**
- Consumes: `RotationOptions` (Task 2).
- Produces:
  - `string? IKeyPool.TryAcquire()` — первый ключ не в cooldown, иначе null.
  - `void IKeyPool.MarkExhausted(string apiKey, TimeSpan? retryAfter)`.
  - `int IKeyPool.Count` — размер пула.

- [ ] **Step 1: Создать тест-дубль времени**

`tests/Comuki.Platform.Routing.Unit.KeyRotation/MutableTimeProvider.cs`:

```csharp
namespace Comuki.Platform.Routing.Unit.KeyRotation;

/// <summary>Минимальный управляемый TimeProvider для тестов (без внешних пакетов).</summary>
public sealed class MutableTimeProvider(DateTimeOffset start) : TimeProvider
{
    private DateTimeOffset now = start;

    public override DateTimeOffset GetUtcNow() => now;

    public void Advance(TimeSpan delta) => now += delta;
}
```

- [ ] **Step 2: Написать падающие тесты**

`tests/Comuki.Platform.Routing.Unit.KeyRotation/KeyPoolTests.cs`:

```csharp
using Comuki.Platform.Routing.Interfaces;
using Comuki.Platform.Routing.Options;
using Comuki.Platform.Routing.Services;
using Microsoft.Extensions.Options;
using Shouldly;
using Xunit;

namespace Comuki.Platform.Routing.Unit.KeyRotation;

public sealed class KeyPoolTests
{
    private static readonly DateTimeOffset Start = new(2026, 6, 23, 12, 0, 0, TimeSpan.Zero);

    private static (KeyPool Pool, MutableTimeProvider Time) CreateSut(
        string[] keys,
        TimeSpan? defaultCooldown = null)
    {
        var options = Microsoft.Extensions.Options.Options.Create(new RotationOptions
        {
            ApiKeys = keys,
            UpstreamUrl = "https://upstream.example",
            DefaultCooldown = defaultCooldown ?? TimeSpan.FromHours(1),
            ExhaustionRules = [new ExhaustionRule { StatusCode = 429 }],
        });
        var time = new MutableTimeProvider(Start);
        return (new KeyPool(options, time), time);
    }

    [Fact]
    public void TryAcquire_ReturnsFirstKey_WhenNoneExhausted()
    {
        var (pool, _) = CreateSut(["a", "b"]);

        pool.TryAcquire().ShouldBe("a");
    }

    [Fact]
    public void TryAcquire_SkipsExhaustedKey()
    {
        var (pool, _) = CreateSut(["a", "b"]);

        pool.MarkExhausted("a", retryAfter: null);

        pool.TryAcquire().ShouldBe("b");
    }

    [Fact]
    public void TryAcquire_ReturnsNull_WhenAllExhausted()
    {
        var (pool, _) = CreateSut(["a", "b"]);

        pool.MarkExhausted("a", retryAfter: null);
        pool.MarkExhausted("b", retryAfter: null);

        pool.TryAcquire().ShouldBeNull();
    }

    [Fact]
    public void TryAcquire_RecoversKey_AfterCooldownElapses()
    {
        var (pool, time) = CreateSut(["a"], defaultCooldown: TimeSpan.FromMinutes(30));

        pool.MarkExhausted("a", retryAfter: null);
        pool.TryAcquire().ShouldBeNull();

        time.Advance(TimeSpan.FromMinutes(31));

        pool.TryAcquire().ShouldBe("a");
    }

    [Fact]
    public void MarkExhausted_UsesRetryAfter_WhenProvided()
    {
        var (pool, time) = CreateSut(["a"], defaultCooldown: TimeSpan.FromHours(1));

        pool.MarkExhausted("a", retryAfter: TimeSpan.FromMinutes(5));

        time.Advance(TimeSpan.FromMinutes(4));
        pool.TryAcquire().ShouldBeNull();

        time.Advance(TimeSpan.FromMinutes(2));
        pool.TryAcquire().ShouldBe("a");
    }

    [Fact]
    public void Count_ReturnsPoolSize()
    {
        var (pool, _) = CreateSut(["a", "b", "c"]);

        pool.Count.ShouldBe(3);
    }
}
```

- [ ] **Step 3: Запустить — убедиться, что падает**

Run: `dotnet run --project tests/Comuki.Platform.Routing.Unit.KeyRotation`
Expected: ошибка компиляции — `IKeyPool` / `KeyPool` не существуют.

- [ ] **Step 4: Создать интерфейс**

`platform/src/feature/Comuki.Platform.Routing/Interfaces/IKeyPool.cs`:

```csharp
namespace Comuki.Platform.Routing.Interfaces;

/// <summary>
/// Пул физических ключей Z.AI с cooldown. Потокобезопасен: несколько
/// воркеров бьют в прокси параллельно. Состояние — в памяти инстанса.
/// </summary>
public interface IKeyPool
{
    /// <summary>Размер пула (общее число ключей, включая в cooldown).</summary>
    int Count { get; }

    /// <summary>Первый ключ не в cooldown, либо null если все в cooldown.</summary>
    string? TryAcquire();

    /// <summary>
    /// Пометить ключ исчерпанным: cooldown до now + retryAfter,
    /// либо до now + DefaultCooldown, если retryAfter не задан. Идемпотентно.
    /// </summary>
    void MarkExhausted(string apiKey, TimeSpan? retryAfter);
}
```

- [ ] **Step 5: Реализовать `KeyPool`**

`platform/src/feature/Comuki.Platform.Routing/Services/KeyPool.cs`:

```csharp
using Comuki.Platform.Routing.Interfaces;
using Comuki.Platform.Routing.Options;
using Microsoft.Extensions.Options;

namespace Comuki.Platform.Routing.Services;

/// <inheritdoc />
public sealed class KeyPool : IKeyPool
{
    private readonly object gate = new();
    private readonly Dictionary<string, DateTimeOffset> cooldownUntil;
    private readonly string[] keys;
    private readonly TimeProvider timeProvider;
    private readonly TimeSpan defaultCooldown;

    public KeyPool(IOptions<RotationOptions> options, TimeProvider timeProvider)
    {
        var value = options.Value;
        this.keys = value.ApiKeys;
        this.defaultCooldown = value.DefaultCooldown;
        this.timeProvider = timeProvider;
        this.cooldownUntil = new Dictionary<string, DateTimeOffset>(StringComparer.Ordinal);
    }

    public int Count => this.keys.Length;

    public string? TryAcquire()
    {
        lock (this.gate)
        {
            var now = this.timeProvider.GetUtcNow();
            foreach (var key in this.keys)
            {
                if (!this.cooldownUntil.TryGetValue(key, out var until) || until <= now)
                {
                    return key;
                }
            }

            return null;
        }
    }

    public void MarkExhausted(string apiKey, TimeSpan? retryAfter)
    {
        lock (this.gate)
        {
            this.cooldownUntil[apiKey] = this.timeProvider.GetUtcNow() + (retryAfter ?? this.defaultCooldown);
        }
    }
}
```

- [ ] **Step 6: Запустить тесты — PASS**

Run: `dotnet run --project tests/Comuki.Platform.Routing.Unit.KeyRotation`
Expected: все тесты PASS.

- [ ] **Step 7: Commit**

```bash
git add platform/src/feature/Comuki.Platform.Routing/Interfaces/IKeyPool.cs \
        platform/src/feature/Comuki.Platform.Routing/Services/KeyPool.cs \
        tests/Comuki.Platform.Routing.Unit.KeyRotation/MutableTimeProvider.cs \
        tests/Comuki.Platform.Routing.Unit.KeyRotation/KeyPoolTests.cs
git commit -m "[hybrid] feat(phase-5): thread-safe KeyPool with cooldown"
```

---

### Task 5: `KeyRotatingForwarder` + транспортные контракты (TDD)

Оркестратор retry-цикла поверх абстракции `IUpstreamSender` — полностью юнит-тестируемое ядро фичи (без реального HTTP).

**Files:**
- Create: `platform/src/feature/Comuki.Platform.Routing/Forwarding/SendOutcome.cs`
- Create: `platform/src/feature/Comuki.Platform.Routing/Forwarding/UpstreamSendResult.cs`
- Create: `platform/src/feature/Comuki.Platform.Routing/Interfaces/IUpstreamSender.cs`
- Create: `platform/src/feature/Comuki.Platform.Routing/Interfaces/IKeyRotatingForwarder.cs`
- Create: `platform/src/feature/Comuki.Platform.Routing/Forwarding/KeyRotatingForwarder.cs`
- Test: `tests/Comuki.Platform.Routing.Unit.KeyRotation/KeyRotatingForwarderTests.cs`

**Interfaces:**
- Consumes: `IKeyPool` (Task 4).
- Produces:
  - `enum SendOutcome { Success, Exhausted, PassedThroughError }`.
  - `sealed record UpstreamSendResult(SendOutcome Outcome, TimeSpan? RetryAfter)` + статические фабрики `Success()`, `Exhausted(TimeSpan?)`, `PassedThroughError()`.
  - `Task<UpstreamSendResult> IUpstreamSender.SendOnceAsync(HttpContext context, string apiKey, CancellationToken ct)`.
  - `Task IKeyRotatingForwarder.ForwardAsync(HttpContext context, CancellationToken ct)`.

- [ ] **Step 1: Написать падающие тесты**

`tests/Comuki.Platform.Routing.Unit.KeyRotation/KeyRotatingForwarderTests.cs`:

```csharp
using Comuki.Platform.Routing.Forwarding;
using Comuki.Platform.Routing.Interfaces;
using Microsoft.AspNetCore.Http;
using Microsoft.Extensions.Logging.Abstractions;
using NSubstitute;
using Shouldly;
using Xunit;

namespace Comuki.Platform.Routing.Unit.KeyRotation;

public sealed class KeyRotatingForwarderTests
{
    private static KeyRotatingForwarder CreateSut(IKeyPool pool, IUpstreamSender sender)
        => new(pool, sender, NullLogger<KeyRotatingForwarder>.Instance);

    [Fact]
    public async Task ForwardAsync_StreamsSuccess_OnFirstLiveKey()
    {
        var pool = Substitute.For<IKeyPool>();
        pool.Count.Returns(2);
        pool.TryAcquire().Returns("live");
        var sender = Substitute.For<IUpstreamSender>();
        sender.SendOnceAsync(Arg.Any<HttpContext>(), "live", Arg.Any<CancellationToken>())
            .Returns(UpstreamSendResult.Success());
        var context = new DefaultHttpContext();

        await CreateSut(pool, sender).ForwardAsync(context, CancellationToken.None);

        pool.DidNotReceive().MarkExhausted(Arg.Any<string>(), Arg.Any<TimeSpan?>());
        await sender.Received(1).SendOnceAsync(Arg.Any<HttpContext>(), "live", Arg.Any<CancellationToken>());
    }

    [Fact]
    public async Task ForwardAsync_RotatesToNextKey_WhenFirstExhausted()
    {
        var pool = Substitute.For<IKeyPool>();
        pool.Count.Returns(2);
        pool.TryAcquire().Returns("dead", "live");
        var sender = Substitute.For<IUpstreamSender>();
        sender.SendOnceAsync(Arg.Any<HttpContext>(), "dead", Arg.Any<CancellationToken>())
            .Returns(UpstreamSendResult.Exhausted(TimeSpan.FromMinutes(5)));
        sender.SendOnceAsync(Arg.Any<HttpContext>(), "live", Arg.Any<CancellationToken>())
            .Returns(UpstreamSendResult.Success());
        var context = new DefaultHttpContext();

        await CreateSut(pool, sender).ForwardAsync(context, CancellationToken.None);

        pool.Received(1).MarkExhausted("dead", TimeSpan.FromMinutes(5));
        await sender.Received(1).SendOnceAsync(Arg.Any<HttpContext>(), "live", Arg.Any<CancellationToken>());
    }

    [Fact]
    public async Task ForwardAsync_Returns503_WhenAllKeysExhausted()
    {
        var pool = Substitute.For<IKeyPool>();
        pool.Count.Returns(1);
        pool.TryAcquire().Returns("dead", _ => null);
        var sender = Substitute.For<IUpstreamSender>();
        sender.SendOnceAsync(Arg.Any<HttpContext>(), "dead", Arg.Any<CancellationToken>())
            .Returns(UpstreamSendResult.Exhausted(null));
        var context = new DefaultHttpContext();
        context.Response.Body = new MemoryStream();

        await CreateSut(pool, sender).ForwardAsync(context, CancellationToken.None);

        context.Response.StatusCode.ShouldBe(StatusCodes.Status503ServiceUnavailable);
    }

    [Fact]
    public async Task ForwardAsync_StopsAndReturns_OnPassThroughError()
    {
        var pool = Substitute.For<IKeyPool>();
        pool.Count.Returns(2);
        pool.TryAcquire().Returns("live");
        var sender = Substitute.For<IUpstreamSender>();
        sender.SendOnceAsync(Arg.Any<HttpContext>(), "live", Arg.Any<CancellationToken>())
            .Returns(UpstreamSendResult.PassedThroughError());
        var context = new DefaultHttpContext();

        await CreateSut(pool, sender).ForwardAsync(context, CancellationToken.None);

        pool.DidNotReceive().MarkExhausted(Arg.Any<string>(), Arg.Any<TimeSpan?>());
        await sender.Received(1).SendOnceAsync(Arg.Any<HttpContext>(), Arg.Any<string>(), Arg.Any<CancellationToken>());
    }

    [Fact]
    public async Task ForwardAsync_Returns503Immediately_WhenPoolEmpty()
    {
        var pool = Substitute.For<IKeyPool>();
        pool.Count.Returns(1);
        pool.TryAcquire().Returns((string?)null);
        var sender = Substitute.For<IUpstreamSender>();
        var context = new DefaultHttpContext();
        context.Response.Body = new MemoryStream();

        await CreateSut(pool, sender).ForwardAsync(context, CancellationToken.None);

        context.Response.StatusCode.ShouldBe(StatusCodes.Status503ServiceUnavailable);
        await sender.DidNotReceive().SendOnceAsync(Arg.Any<HttpContext>(), Arg.Any<string>(), Arg.Any<CancellationToken>());
    }
}
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `dotnet run --project tests/Comuki.Platform.Routing.Unit.KeyRotation`
Expected: ошибки компиляции — типы `SendOutcome` / `UpstreamSendResult` / `IUpstreamSender` / `KeyRotatingForwarder` не существуют.

- [ ] **Step 3: Создать `SendOutcome.cs`**

`platform/src/feature/Comuki.Platform.Routing/Forwarding/SendOutcome.cs`:

```csharp
namespace Comuki.Platform.Routing.Forwarding;

/// <summary>Исход одной попытки форварда на апстрим.</summary>
public enum SendOutcome
{
    /// <summary>Ответ успешно стримнут клиенту. Цикл завершён.</summary>
    Success,

    /// <summary>Квота ключа исчерпана. Клиенту ничего не записано — нужен retry на следующем ключе.</summary>
    Exhausted,

    /// <summary>Не-quota ошибка (или сетевой сбой) уже проброшена клиенту. Цикл завершён.</summary>
    PassedThroughError,
}
```

- [ ] **Step 4: Создать `UpstreamSendResult.cs`**

`platform/src/feature/Comuki.Platform.Routing/Forwarding/UpstreamSendResult.cs`:

```csharp
namespace Comuki.Platform.Routing.Forwarding;

/// <summary>Результат попытки форварда: исход + (для Exhausted) Retry-After.</summary>
public sealed record UpstreamSendResult(SendOutcome Outcome, TimeSpan? RetryAfter)
{
    public static UpstreamSendResult Success() => new(SendOutcome.Success, null);

    public static UpstreamSendResult Exhausted(TimeSpan? retryAfter) => new(SendOutcome.Exhausted, retryAfter);

    public static UpstreamSendResult PassedThroughError() => new(SendOutcome.PassedThroughError, null);
}
```

- [ ] **Step 5: Создать интерфейсы**

`platform/src/feature/Comuki.Platform.Routing/Interfaces/IUpstreamSender.cs`:

```csharp
using Comuki.Platform.Routing.Forwarding;
using Microsoft.AspNetCore.Http;

namespace Comuki.Platform.Routing.Interfaces;

/// <summary>
/// Одна попытка форварда запроса на апстрим Z.AI с конкретным ключом.
/// Реализация решает: успех (стримит клиенту), исчерпание (ничего не пишет),
/// либо не-quota ошибка (пробрасывает клиенту).
/// </summary>
public interface IUpstreamSender
{
    Task<UpstreamSendResult> SendOnceAsync(HttpContext context, string apiKey, CancellationToken cancellationToken);
}
```

`platform/src/feature/Comuki.Platform.Routing/Interfaces/IKeyRotatingForwarder.cs`:

```csharp
using Microsoft.AspNetCore.Http;

namespace Comuki.Platform.Routing.Interfaces;

/// <summary>
/// Точка входа прокси: форвардит запрос агента на Z.AI, прозрачно ротируя
/// исчерпанные ключи. Пишет ответ (или ошибку) напрямую в HttpContext.Response.
/// </summary>
public interface IKeyRotatingForwarder
{
    Task ForwardAsync(HttpContext context, CancellationToken cancellationToken);
}
```

- [ ] **Step 6: Реализовать `KeyRotatingForwarder`**

`platform/src/feature/Comuki.Platform.Routing/Forwarding/KeyRotatingForwarder.cs`:

```csharp
using Comuki.Platform.Routing.Interfaces;
using Microsoft.AspNetCore.Http;
using Microsoft.Extensions.Logging;

namespace Comuki.Platform.Routing.Forwarding;

/// <inheritdoc />
public sealed partial class KeyRotatingForwarder(
    IKeyPool keyPool,
    IUpstreamSender sender,
    ILogger<KeyRotatingForwarder> logger) : IKeyRotatingForwarder
{
    private const string OverloadedBody =
        """{"type":"error","error":{"type":"overloaded_error","message":"All Z.AI keys are in cooldown."}}""";

    public async Task ForwardAsync(HttpContext context, CancellationToken cancellationToken)
    {
        // Буферизуем тело запроса, чтобы повторить его на следующем ключе.
        context.Request.EnableBuffering();

        var maxAttempts = keyPool.Count;
        for (var attempt = 0; attempt < maxAttempts; attempt++)
        {
            if (context.Request.Body.CanSeek)
            {
                context.Request.Body.Position = 0;
            }

            var key = keyPool.TryAcquire();
            if (key is null)
            {
                break;
            }

            var result = await sender.SendOnceAsync(context, key, cancellationToken).ConfigureAwait(false);
            switch (result.Outcome)
            {
                case SendOutcome.Success:
                case SendOutcome.PassedThroughError:
                    return;

                case SendOutcome.Exhausted:
                    keyPool.MarkExhausted(key, result.RetryAfter);
                    LogKeyRotated(logger, result.RetryAfter);
                    continue;

                default:
                    return;
            }
        }

        await WriteAllKeysExhaustedAsync(context, cancellationToken).ConfigureAwait(false);
    }

    private static async Task WriteAllKeysExhaustedAsync(HttpContext context, CancellationToken cancellationToken)
    {
        context.Response.StatusCode = StatusCodes.Status503ServiceUnavailable;
        context.Response.ContentType = "application/json";
        await context.Response.WriteAsync(OverloadedBody, cancellationToken).ConfigureAwait(false);
    }

    [LoggerMessage(EventId = 1, Level = LogLevel.Warning,
        Message = "key_rotated: Z.AI key exhausted, cooldown retry-after {RetryAfter}")]
    private static partial void LogKeyRotated(ILogger logger, TimeSpan? retryAfter);
}
```

- [ ] **Step 7: Запустить тесты — PASS**

Run: `dotnet run --project tests/Comuki.Platform.Routing.Unit.KeyRotation`
Expected: все тесты PASS.

- [ ] **Step 8: Commit**

```bash
git add platform/src/feature/Comuki.Platform.Routing/Forwarding \
        platform/src/feature/Comuki.Platform.Routing/Interfaces/IUpstreamSender.cs \
        platform/src/feature/Comuki.Platform.Routing/Interfaces/IKeyRotatingForwarder.cs \
        tests/Comuki.Platform.Routing.Unit.KeyRotation/KeyRotatingForwarderTests.cs
git commit -m "[hybrid] feat(phase-5): KeyRotatingForwarder retry orchestration"
```

---

### Task 6: `YarpUpstreamSender` + `RotatingTransformer` (реальная YARP-обвязка)

Реальная реализация `IUpstreamSender` поверх `IHttpForwarder`. Изолированного юнит-теста нет (фидлерная HTTP-склейка) — покрывается интеграционным тестом в Task 9.

**Files:**
- Create: `platform/src/feature/Comuki.Platform.Routing/Forwarding/RotatingTransformer.cs`
- Create: `platform/src/feature/Comuki.Platform.Routing/Forwarding/YarpUpstreamSender.cs`

**Interfaces:**
- Consumes: `IQuotaExhaustionDetector` (Task 3), `RotationOptions` (Task 2), `UpstreamSendResult`/`SendOutcome` (Task 5), YARP `IHttpForwarder`/`HttpTransformer`/`ForwarderError`.
- Produces: `YarpUpstreamSender : IUpstreamSender` (для DI в Task 7).

- [ ] **Step 1: Создать `RotatingTransformer`**

`platform/src/feature/Comuki.Platform.Routing/Forwarding/RotatingTransformer.cs`:

```csharp
using Comuki.Platform.Routing.Interfaces;
using Microsoft.AspNetCore.Http;
using Yarp.ReverseProxy.Forwarder;

namespace Comuki.Platform.Routing.Forwarding;

/// <summary>
/// Per-request трансформер: подменяет ключ на исходящем запросе и инспектирует
/// ответ апстрима. Успех (2xx) стримит как есть; non-2xx читает (маленькое) тело
/// и через детектор решает — исчерпание (suppress, retry) или реальная ошибка
/// (пробросить клиенту). Результат — в <see cref="Result"/>.
/// </summary>
internal sealed class RotatingTransformer(string apiKey, IQuotaExhaustionDetector detector) : HttpTransformer
{
    public UpstreamSendResult Result { get; private set; } = UpstreamSendResult.PassedThroughError();

    public override async ValueTask TransformRequestAsync(
        HttpContext httpContext,
        HttpRequestMessage proxyRequest,
        string destinationPrefix,
        CancellationToken cancellationToken)
    {
        await base.TransformRequestAsync(httpContext, proxyRequest, destinationPrefix, cancellationToken)
            .ConfigureAwait(false);

        // Z.AI / Anthropic-совместимый эндпоинт принимает ключ в обоих заголовках.
        proxyRequest.Headers.Remove("Authorization");
        proxyRequest.Headers.TryAddWithoutValidation("Authorization", $"Bearer {apiKey}");
        proxyRequest.Headers.Remove("x-api-key");
        proxyRequest.Headers.TryAddWithoutValidation("x-api-key", apiKey);
    }

    public override async ValueTask<bool> TransformResponseAsync(
        HttpContext httpContext,
        HttpResponseMessage? proxyResponse,
        CancellationToken cancellationToken)
    {
        if (proxyResponse is null)
        {
            // Сетевой/форвард сбой — апстрим не ответил. Forwarder уже выставит ошибку клиенту.
            this.Result = UpstreamSendResult.PassedThroughError();
            return false;
        }

        var status = (int)proxyResponse.StatusCode;
        if (status is >= 200 and < 300)
        {
            this.Result = UpstreamSendResult.Success();
            return await base.TransformResponseAsync(httpContext, proxyResponse, cancellationToken)
                .ConfigureAwait(false);
        }

        var body = await proxyResponse.Content.ReadAsStringAsync(cancellationToken).ConfigureAwait(false);
        if (detector.IsExhausted(status, body))
        {
            this.Result = UpstreamSendResult.Exhausted(ParseRetryAfter(proxyResponse));
            return false; // ничего не пишем клиенту — даём шанс retry
        }

        // Реальная не-quota ошибка — пробрасываем клиенту (тело уже прочитано).
        this.Result = UpstreamSendResult.PassedThroughError();
        httpContext.Response.StatusCode = status;
        if (proxyResponse.Content.Headers.ContentType is { } contentType)
        {
            httpContext.Response.ContentType = contentType.ToString();
        }

        await httpContext.Response.WriteAsync(body, cancellationToken).ConfigureAwait(false);
        return false;
    }

    private static TimeSpan? ParseRetryAfter(HttpResponseMessage response)
    {
        var retryAfter = response.Headers.RetryAfter;
        if (retryAfter is null)
        {
            return null;
        }

        if (retryAfter.Delta is { } delta)
        {
            return delta;
        }

        if (retryAfter.Date is { } date)
        {
            var remaining = date - DateTimeOffset.UtcNow;
            return remaining > TimeSpan.Zero ? remaining : null;
        }

        return null;
    }
}
```

- [ ] **Step 2: Создать `YarpUpstreamSender`**

`platform/src/feature/Comuki.Platform.Routing/Forwarding/YarpUpstreamSender.cs`:

```csharp
using Comuki.Platform.Routing.Interfaces;
using Comuki.Platform.Routing.Options;
using Microsoft.AspNetCore.Http;
using Microsoft.Extensions.Options;
using Yarp.ReverseProxy.Forwarder;

namespace Comuki.Platform.Routing.Forwarding;

/// <inheritdoc />
public sealed class YarpUpstreamSender(
    IHttpForwarder forwarder,
    IQuotaExhaustionDetector detector,
    HttpMessageInvoker httpClient,
    IOptions<RotationOptions> options) : IUpstreamSender
{
    private readonly string upstreamUrl = options.Value.UpstreamUrl;

    public async Task<UpstreamSendResult> SendOnceAsync(
        HttpContext context,
        string apiKey,
        CancellationToken cancellationToken)
    {
        var transformer = new RotatingTransformer(apiKey, detector);

        await forwarder.SendAsync(context, this.upstreamUrl, this.httpClient, ForwarderRequestConfig.Empty, transformer)
            .ConfigureAwait(false);

        return transformer.Result;
    }
}
```

- [ ] **Step 3: Verify — сборка библиотеки**

Run: `dotnet build platform/src/feature/Comuki.Platform.Routing/Comuki.Platform.Routing.csproj`
Expected: `0 Warning(s) 0 Error(s)`.

> Если анализатор требует `ConfigureAwait` на всех await — он уже проставлен. Если `ForwarderRequestConfig.Empty` недоступен в этой версии YARP, заменить на `new ForwarderRequestConfig()`.

- [ ] **Step 4: Commit**

```bash
git add platform/src/feature/Comuki.Platform.Routing/Forwarding/RotatingTransformer.cs \
        platform/src/feature/Comuki.Platform.Routing/Forwarding/YarpUpstreamSender.cs
git commit -m "[hybrid] feat(phase-5): YarpUpstreamSender + RotatingTransformer (IHttpForwarder glue)"
```

---

### Task 7: `RoutingInstaller` (DI-проводка)

Единый Installer проекта `Routing` — регистрирует Options (с валидацией), пул, детектор, sender, forwarder и YARP-инфраструктуру.

**Files:**
- Create: `platform/src/feature/Comuki.Platform.Routing/Installers/RoutingInstaller.cs`

**Interfaces:**
- Consumes: всё из Task 2–6.
- Produces: `IServiceCollection RoutingInstaller.AddRoutingCore(this IServiceCollection, IConfiguration)`.

- [ ] **Step 1: Создать `RoutingInstaller`**

`platform/src/feature/Comuki.Platform.Routing/Installers/RoutingInstaller.cs`:

```csharp
using System.Net;
using Comuki.Platform.Routing.Forwarding;
using Comuki.Platform.Routing.Interfaces;
using Comuki.Platform.Routing.Options;
using Microsoft.Extensions.Configuration;
using Microsoft.Extensions.DependencyInjection;
using Yarp.ReverseProxy.Forwarder;

namespace Comuki.Platform.Routing.Installers;

/// <summary>
/// DI-регистрация модуля Routing: ротация ключей Z.AI поверх YARP IHttpForwarder.
/// </summary>
public static class RoutingInstaller
{
    public static IServiceCollection AddRoutingCore(
        this IServiceCollection services,
        IConfiguration configuration)
    {
        services.AddOptions<RotationOptions>()
            .Bind(configuration.GetSection(RotationOptions.SectionName))
            .ValidateDataAnnotations()
            .Validate(
                options => options.ExhaustionRules.All(
                    rule => rule.StatusCode is not null || !string.IsNullOrWhiteSpace(rule.BodyContains)),
                "Each exhaustion rule must set StatusCode or BodyContains.")
            .ValidateOnStart();

        services.AddSingleton(TimeProvider.System);
        services.AddSingleton<IKeyPool, KeyPool>();
        services.AddSingleton<IQuotaExhaustionDetector, QuotaExhaustionDetector>();
        services.AddSingleton<IUpstreamSender, YarpUpstreamSender>();
        services.AddSingleton<IKeyRotatingForwarder, KeyRotatingForwarder>();

        // YARP direct forwarding: IHttpForwarder + выделенный HttpMessageInvoker.
        services.AddHttpForwarder();
        services.AddSingleton(CreateForwarderHttpClient());

        return services;
    }

    // HttpMessageInvoker для IHttpForwarder — рекомендованная YARP конфигурация
    // (без авто-редиректов/декомпрессии/куки; прокси пробрасывает поток как есть).
    private static HttpMessageInvoker CreateForwarderHttpClient()
        => new(new SocketsHttpHandler
        {
            UseProxy = false,
            AllowAutoRedirect = false,
            AutomaticDecompression = DecompressionMethods.None,
            UseCookies = false,
            EnableMultipleHttp2Connections = true,
        });
}
```

- [ ] **Step 2: Verify — сборка**

Run: `dotnet build platform/src/feature/Comuki.Platform.Routing/Comuki.Platform.Routing.csproj`
Expected: `0 Warning(s) 0 Error(s)`.

- [ ] **Step 3: Commit**

```bash
git add platform/src/feature/Comuki.Platform.Routing/Installers/RoutingInstaller.cs
git commit -m "[hybrid] feat(phase-5): RoutingInstaller DI wiring"
```

---

### Task 8: Scaffold `Comuki.Platform.Proxy` (тонкий YARP-хост)

Веб-хост: поднимает Kestrel, маппит catch-all на форвардер, /health. Деливерабл: хост стартует, `/health` → 200.

**Files:**
- Create: `platform/src/application/internal/Comuki.Platform.Proxy/Comuki.Platform.Proxy.csproj`
- Create: `platform/src/application/internal/Comuki.Platform.Proxy/Program.cs`
- Create: `platform/src/application/internal/Comuki.Platform.Proxy/appsettings.json`
- Modify: `comuki.slnx` (добавить в `<Folder Name="/platform/src/application/internal/">`)

**Interfaces:**
- Consumes: `RoutingInstaller.AddRoutingCore` (Task 7), `IKeyRotatingForwarder` (Task 5).
- Produces: запускаемый хост + `public partial class Program` для `WebApplicationFactory` (Task 9).

- [ ] **Step 1: Создать csproj**

`platform/src/application/internal/Comuki.Platform.Proxy/Comuki.Platform.Proxy.csproj`:

```xml
<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <RootNamespace>Comuki.Platform.Proxy</RootNamespace>
    <AssemblyName>Comuki.Platform.Proxy</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\..\feature\Comuki.Platform.Routing\Comuki.Platform.Routing.csproj" />
  </ItemGroup>

</Project>
```

- [ ] **Step 2: Создать `appsettings.json`**

`platform/src/application/internal/Comuki.Platform.Proxy/appsettings.json`:

```json
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Routing": {
    "Rotation": {
      "UpstreamUrl": "https://api.z.ai",
      "DefaultCooldown": "01:00:00",
      "ApiKeys": [],
      "ExhaustionRules": [
        { "StatusCode": 429 },
        { "BodyContains": "insufficient balance" },
        { "BodyContains": "quota" }
      ]
    }
  }
}
```

> `ApiKeys` пуст в репозитории — реальные ключи приходят из env / секрет-менеджера (например, `Routing__Rotation__ApiKeys__0`). Пустой пул валится на старте (Required + MinLength(1)) — это намеренный fail-fast; для локального запуска задать ключи через env/user-secrets.

- [ ] **Step 3: Создать `Program.cs`**

`platform/src/application/internal/Comuki.Platform.Proxy/Program.cs`:

```csharp
using Comuki.Platform.Routing.Installers;
using Comuki.Platform.Routing.Interfaces;

var builder = WebApplication.CreateBuilder(args);

builder.Host.UseDefaultServiceProvider((context, options) =>
{
    options.ValidateScopes = context.HostingEnvironment.IsDevelopment();
    options.ValidateOnBuild = true;
});

builder.Services.AddRoutingCore(builder.Configuration);

var app = builder.Build();

app.MapGet("/health", () => Results.Ok(new { status = "ok" }));

// Catch-all: всё остальное идёт через форвардер с ротацией ключей.
// Литеральный /health имеет приоритет над catch-all в маршрутизации.
app.Map(
    "/{**catchAll}",
    (HttpContext context, IKeyRotatingForwarder forwarder, CancellationToken cancellationToken)
        => forwarder.ForwardAsync(context, cancellationToken));

await app.RunAsync();

// Доступ для WebApplicationFactory в интеграционных тестах.
public partial class Program;
```

- [ ] **Step 4: Добавить в `comuki.slnx`**

В `<Folder Name="/platform/src/application/internal/">` добавить (после Translator):

```xml
    <Project Path="platform/src/application/internal/Comuki.Platform.Proxy/Comuki.Platform.Proxy.csproj" />
```

- [ ] **Step 5: Verify — csproj⇄slnx, сборка, запуск /health**

Run:
```bash
diff <(find platform/src tests -name '*.csproj' | sort) \
     <(grep -oE 'Path="[^"]+\.csproj"' comuki.slnx | sed 's/Path="//;s/"$//' | sort)
dotnet build comuki.slnx
```
Expected: diff пустой; build → `0 Warning(s) 0 Error(s)`.

Затем smoke-проверка хоста (ключи через env, чтобы пройти валидацию старта):
```bash
Routing__Rotation__ApiKeys__0=dummy dotnet run --project platform/src/application/internal/Comuki.Platform.Proxy &
sleep 5
curl -s http://localhost:5000/health
kill %1
```
Expected: `{"status":"ok"}` (порт может отличаться — взять из вывода `Now listening on:`).

- [ ] **Step 6: Commit**

```bash
git add platform/src/application/internal/Comuki.Platform.Proxy comuki.slnx
git commit -m "[hybrid] feat(phase-5): thin Comuki.Platform.Proxy YARP host"
```

---

### Task 9: Интеграционный тест ротации (E2E)

Поднимаем прокси через `WebApplicationFactory` + фейковый «Z.AI» апстрим. Первый ключ → 429, второй → успех. Проверяем: клиент получил успешный ответ (ротация прозрачна).

**Files:**
- Create: `tests/Comuki.Platform.Proxy.Integration.Rotation/Comuki.Platform.Proxy.Integration.Rotation.csproj`
- Create: `tests/Comuki.Platform.Proxy.Integration.Rotation/FakeUpstream.cs`
- Create: `tests/Comuki.Platform.Proxy.Integration.Rotation/KeyRotationEndToEndTests.cs`
- Modify: `comuki.slnx` (добавить в `<Folder Name="/tests/">`)

**Interfaces:**
- Consumes: `Comuki.Platform.Proxy` `Program` (Task 8).

- [ ] **Step 1: Создать csproj**

`tests/Comuki.Platform.Proxy.Integration.Rotation/Comuki.Platform.Proxy.Integration.Rotation.csproj`:

```xml
<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <RootNamespace>Comuki.Platform.Proxy.Integration.Rotation</RootNamespace>
    <AssemblyName>Comuki.Platform.Proxy.Integration.Rotation</AssemblyName>
    <IsPackable>false</IsPackable>
    <IsTestProject>true</IsTestProject>
    <UseMicrosoftTestingPlatformRunner>true</UseMicrosoftTestingPlatformRunner>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit.v3" />
    <PackageReference Include="Shouldly" />
    <PackageReference Include="Microsoft.AspNetCore.Mvc.Testing" />
    <PackageReference Include="coverlet.collector" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\platform\src\application\internal\Comuki.Platform.Proxy\Comuki.Platform.Proxy.csproj" />
  </ItemGroup>

</Project>
```

> `Microsoft.NET.Sdk.Web` в тест-проекте нужен, чтобы `WebApplicationFactory` и фейковый апстрим имели полный ASP.NET Core хостинг.

Добавить в `comuki.slnx` в `<Folder Name="/tests/">`:
```xml
    <Project Path="tests/Comuki.Platform.Proxy.Integration.Rotation/Comuki.Platform.Proxy.Integration.Rotation.csproj" />
```

- [ ] **Step 2: Создать фейковый апстрим**

`tests/Comuki.Platform.Proxy.Integration.Rotation/FakeUpstream.cs`:

```csharp
using Microsoft.AspNetCore.Builder;
using Microsoft.AspNetCore.Http;

namespace Comuki.Platform.Proxy.Integration.Rotation;

/// <summary>
/// Минимальный фейковый Z.AI: на ключ "dead" отвечает 429 (quota),
/// на ключ "live" — 200 с телом. Слушает на динамическом порту.
/// </summary>
public sealed class FakeUpstream : IAsyncDisposable
{
    private readonly WebApplication app;

    private FakeUpstream(WebApplication app) => this.app = app;

    public string Url { get; private set; } = string.Empty;

    public static async Task<FakeUpstream> StartAsync()
    {
        var builder = WebApplication.CreateBuilder();
        builder.WebHost.UseUrls("http://127.0.0.1:0"); // динамический порт
        var app = builder.Build();

        app.Map("/{**catchAll}", async (HttpContext context) =>
        {
            var auth = context.Request.Headers.Authorization.ToString();
            if (auth.Contains("dead", StringComparison.Ordinal))
            {
                context.Response.StatusCode = StatusCodes.Status429TooManyRequests;
                await context.Response.WriteAsync("""{"error":"rate_limit_exceeded"}""");
                return;
            }

            context.Response.StatusCode = StatusCodes.Status200OK;
            context.Response.ContentType = "application/json";
            await context.Response.WriteAsync("""{"type":"message","content":"ok-from-live-key"}""");
        });

        await app.StartAsync();

        var instance = new FakeUpstream(app)
        {
            Url = app.Urls.First(),
        };
        return instance;
    }

    public async ValueTask DisposeAsync() => await this.app.DisposeAsync();
}
```

- [ ] **Step 3: Написать E2E тест**

`tests/Comuki.Platform.Proxy.Integration.Rotation/KeyRotationEndToEndTests.cs`:

```csharp
using System.Net;
using Microsoft.AspNetCore.Mvc.Testing;
using Microsoft.Extensions.DependencyInjection;
using Shouldly;
using Xunit;

namespace Comuki.Platform.Proxy.Integration.Rotation;

public sealed class KeyRotationEndToEndTests
{
    [Fact]
    public async Task DeadKeyFirst_RotatesToLiveKey_ClientSeesSuccess()
    {
        await using var upstream = await FakeUpstream.StartAsync();

        using var factory = new WebApplicationFactory<Program>()
            .WithWebHostBuilder(webHost =>
            {
                webHost.UseSetting("Routing:Rotation:UpstreamUrl", upstream.Url);
                webHost.UseSetting("Routing:Rotation:DefaultCooldown", "01:00:00");
                webHost.UseSetting("Routing:Rotation:ApiKeys:0", "dead");
                webHost.UseSetting("Routing:Rotation:ApiKeys:1", "live");
                webHost.UseSetting("Routing:Rotation:ExhaustionRules:0:StatusCode", "429");
            });

        using var client = factory.CreateClient();

        using var response = await client.PostAsync(
            "/v1/messages",
            new StringContent("""{"model":"glm","messages":[]}"""));

        response.StatusCode.ShouldBe(HttpStatusCode.OK);
        var body = await response.Content.ReadAsStringAsync();
        body.ShouldContain("ok-from-live-key");
    }

    [Fact]
    public async Task AllKeysDead_ClientGets503()
    {
        await using var upstream = await FakeUpstream.StartAsync();

        using var factory = new WebApplicationFactory<Program>()
            .WithWebHostBuilder(webHost =>
            {
                webHost.UseSetting("Routing:Rotation:UpstreamUrl", upstream.Url);
                webHost.UseSetting("Routing:Rotation:DefaultCooldown", "01:00:00");
                webHost.UseSetting("Routing:Rotation:ApiKeys:0", "dead");
                webHost.UseSetting("Routing:Rotation:ExhaustionRules:0:StatusCode", "429");
            });

        using var client = factory.CreateClient();

        using var response = await client.PostAsync(
            "/v1/messages",
            new StringContent("""{"model":"glm","messages":[]}"""));

        response.StatusCode.ShouldBe(HttpStatusCode.ServiceUnavailable);
    }
}
```

- [ ] **Step 4: Запустить — убедиться, что падает (Program ещё не partial / проект не собран)**

Run: `dotnet run --project tests/Comuki.Platform.Proxy.Integration.Rotation`
Expected: либо компиляция падает (если Task 8 `public partial class Program` забыт), либо тесты гоняются. Если падает на `Program` недоступен — проверить Task 8 Step 3.

- [ ] **Step 5: Запустить тесты — PASS**

Run: `dotnet run --project tests/Comuki.Platform.Proxy.Integration.Rotation`
Expected: оба теста PASS.

- [ ] **Step 6: Full solution verify**

Run:
```bash
diff <(find platform/src tests -name '*.csproj' | sort) \
     <(grep -oE 'Path="[^"]+\.csproj"' comuki.slnx | sed 's/Path="//;s/"$//' | sort)
dotnet build comuki.slnx
dotnet run --project tests/Comuki.Platform.Routing.Unit.KeyRotation
dotnet run --project tests/Comuki.Platform.Proxy.Integration.Rotation
```
Expected: diff пустой; build 0/0; все тесты PASS.

- [ ] **Step 7: Commit**

```bash
git add tests/Comuki.Platform.Proxy.Integration.Rotation comuki.slnx
git commit -m "[hybrid] test(phase-5): E2E key rotation integration test"
```

---

## Заметки для реализатора

- **Версия YARP / API.** План на `Yarp.ReverseProxy` 2.3.0. Если сигнатуры `IHttpForwarder.SendAsync` / `HttpTransformer` / `ForwarderRequestConfig` отличаются — свериться с актуальной докой (context7: `Yarp.ReverseProxy`) и подправить вызовы; контракт фичи (`IUpstreamSender`/`KeyRotatingForwarder`) при этом не меняется.
- **Точные сигналы Z.AI.** `ExhaustionRules` в `appsettings.json` — стартовый набор (429 + «insufficient balance» + «quota»). Уточнить по реальным ответам Z.AI/GLM и дополнить список — менять только конфиг, не код.
- **Стриминг (SSE).** Успешные ответы (2xx) стримятся через `base.TransformResponseAsync` без буферизации — важно для длинных SSE-ответов pi. Буферизуется только тело запроса (для повтора) и маленькие тела ошибок (для детекции).
- **Granularity тестов.** Ядро (detector, pool, forwarder-orchestrator) — юнит-тесты (Task 3–5). YARP-склейка (Task 6) намеренно без изолированного юнит-теста — её корректность проверяет E2E (Task 9).

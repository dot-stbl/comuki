# Build context hygiene for deploy/worker.Dockerfile and deploy/hybrid/*.
# `agents` is NOT excluded: the hybrid worker image COPYs the agents
# workspace (comuki-agent-core + comuki-worker-sdk) into the image.
.git
.agents
dashboard
control-plane
tests
openspec
**/bin
**/obj
**/*.user

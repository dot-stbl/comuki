# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**Основной: дежурный по рою** — инженер команды Comuki, у которого рой идёт прямо
сейчас. Вкладка открыта весь рабочий день; он возвращается к ней каждые несколько
минут между собственными задачами. Он не изучает дашборд — он его сканирует, отвечая
на один вопрос: «нужен ли я сейчас и где». Вмешивается точечно (аппрув плана, разбор
эскалации, отмена прогона, постановка новой задачи) и уходит обратно.

Постановщик задач (разработчик продуктовой команды) и владелец качества (тимлид)
пользуются той же поверхностью.

**Роли зафиксированы** (RBAC, выдаются назначением; сами роли — в коде):
`platform-admin` · `operator` · `project-admin` · `member` · `viewer` · `approver`.
Матрица экранов на роли — в `.agents/docs/product/comuki-fe-requirements.md` §18.

Два правила разграничения, и они разные по смыслу: **пункт навигации, недоступный
роли, прячется; действие, недоступное роли, остаётся видимым, но заблокированным
с объяснением, чего не хватает.** Первое убирает шум, второе — учит систему.

**Проект — атрибут, а не скоуп.** Дежурный смотрит рой целиком: в списках проект
стоит колонкой и фильтром рядом с app и profile, и переключателя проекта в хроме
нет. Проекты заводят и настраивают в собственном разделе, как любую другую
сущность платформы.

**Рейка делится на два яруса.** Сверху ежедневное — интейк, наблюдение, настройка;
под перегородкой платформенное: Projects, Identity, Compute, Models,
Observability. Внизу рейки — блок пользователя.

## Product Purpose

Comuki — платформа, где ведущая модель декомпозирует задачу и дирижирует роем
эфемерных воркеров в контейнерах, которые пишут, тестируют и готовят к выкату фичи
для *других* проектов. Собственного продуктового кода клиентов в этом репозитории нет —
только платформа, агентские SDK и operational UI.

Дашборд — управляющая поверхность платформы: **полный пульт, а не витрина**. Из него
ставят задачи рою, аппрувят планы, разбирают эскалации, останавливают прогоны и
вмешиваются в живые, крутят квоты, флаги, бюджеты и маршрутизацию моделей.

**Но не правят промпты и правила.** Профили воркеров, правила и скиллы живут в git
клиента и приходят по пинованному ref — UI показывает и связывает, а меняет их
коммит. Это разделение обязательно: живой правкой в UI сюда лезть нельзя, иначе
прогон окажется собран из двух сводов правил.

Успех: дежурный за две секунды видит, требуется ли его решение, и принимает его,
не покидая экрана.

## Positioning

«LLM предлагает — система распоряжается»: детерминированный control plane, LLM только
в планировании, контексте и ревью. Три механизма, которые соседний продукт не скопирует
честно:

1. **Агенты stateless и эфемерны.** Контейнер — это функция `(бриф + код) → (дифф + отчёт)`.
   Снос по дизайну, не баг; единственное условие — извлечь результат до сноса.
2. **Защита от слопа как принуждаемое правило,** а не просьба к модели: проверенные
   скиллы-рецепты, жёсткие анализаторы, дизайн-система, хуки принуждения, гейт верификации.
3. **Собственный модельный прокси с виртуальным ключом-capability:** маршрут, бюджет,
   модели, скоуп и TTL внутри самого ключа; утёкший ключ почти бесполезен.

Человек всегда замыкает петлю — авто-самоизменения управляющей плоскости нет.

## Operating Context

- **Прогон = граф work items, а не конвейер стадий.** Ведущая модель декомпозирует
  задачу в граф запусков профилей с зависимостями и **сама придумывает названия
  шагов**. Двух одинаковых планов может не быть вовсе.
  - **Профиль — идентичность.** Закрытый каталог, объявленный в git клиента; мозг
    выбирает только из существующего. Это единственная ось со стабильной
    идентичностью между прогонами, и единственная, по которой можно агрегировать:
    упёршийся профиль чинится правкой этого профиля в git.
  - **Название шага — проза.** Свободный текст от языковой модели. Показывается в
    списке и в графе прогона, но никогда не служит ключом: агрегация по выдуманным
    именам даёт сорок колонок, за которыми стоит шесть вещей.
  - Фиксированный каталог стадий — **опция проекта** (playbook), а не устройство
    платформы. Если он есть, колонки просто называются стадиями.
- Шаг может упереться в гейт верификации; после N провалов — эскалация к человеку.
- **Масштаб v1: 50–200 одновременных прогонов, десятки воркеров, несколько команд.**
  Плоский список без агрегации, группировки и фильтров на этом объёме бесполезен.
- **Шесть статусов прогона:** running, success, failed, waiting, queued, escalated.
- **Роли моделей, не вендоры:** ведущая (план / контракт / ревью / ремонт) и рабочая
  (воркеры, исполняющие work items). Физическая модель за ролью резолвится в проксе,
  одной таблицей.
- **Run-id = trace-id.** Артефакты прогона (summary, диффы, скриншоты, логи, пины версий)
  лежат в MinIO; метрики и логи — в Victoria через OpenTelemetry.
- **Управляющая плоскость версионируется как код.** Версия правил пинится на прогон
  (git-ref); правка действует со следующих запусков, перезапуск = новый прогон.
- Всё self-hosted. Прод касается только последняя стадия за гейтом.

## Capabilities and Constraints

Разделы v1 (полный перечень с уровнями и ролями — в
`.agents/docs/product/comuki-fe-requirements.md` §0): Home/Attention · Runs ·
Queue/Workers · Inbox · Sources · Chat · Plan approve · Knowledge · Verify ·
Cost/Usage · Settings (live и git-linked) · Identity · Projects · Observability ·
Auth.

**Конфиг делится на три источника, и это должно быть видно на экране:**
git клиента (профили, admission, playbooks) · UI с живой перезагрузкой (квоты,
флаги, approve on/off, debug, бюджеты) · env (только секреты). Настройка, которую
UI показывает, но не может менять, обязана объяснять, где она живёт.

Технические:

- **FE:** React 19 + Vite + TypeScript strict + Tailwind v4 + shadcn/ui (55 компонентов) +
  TanStack Router + TanStack Query. Типы и хуки генерируются Kubb из `openapi-v1.json` —
  контракт бэкенда единственный источник, руками не пишется.
- **BE:** C# / .NET 10 — оркестратор, YARP-прокси, Translator (AOT). Postgres (состояние,
  очередь, вектора), MinIO, Nexus.
- **Швы:** gRPC (Translator ↔ оркестратор), MCP (агент ↔ знания), SignalR (дашборд ↔ сервер).
  Реального стрима в дашборде ещё нет — данные для UI пока приходят обычными запросами.
- Порог покрытия тестами 70% строк, на фронте и на бэке.
- Агентские сессии не поднимают dev-сервер: `bun run dev` запускает человек.

*Не решено и не должно выдумываться:* размещение chat host (отдельный сервис или
модуль) · DSL фильтров admission · пороги петли обучения из §9 scope-draft.

*Закрыто с прошлой редакции:* роли и права (§18 FE-требований), мультипроектность
(переключатель в шапке), аутентификация (§16: локальная + OIDC), интейк из пяти
внешних трекеров плюс native и chat.

## Brand Commitments

- Имя продукта — **Comuki**.
- **Интерфейсная лексика английская и терминальная.** Русский остаётся только в контенте,
  приходящем от бэкенда и людей: названия задач, тикеты, брифы. Зафиксировано в текущей
  дизайн-системе (§15) и обязательно.
- Проектная документация и общение с командой — на русском.
- **Визуальный мир заменён и зафиксирован.** Действующая авторитетность —
  `DESIGN.md` в ветке дашборда (мир «Диспетчерская»: тёмный пол, бесцветный хром,
  цвет живёт только в потоке; Archivo несёт смысл, JetBrains Mono — значения).
  `.agents/docs/design-system/` описывает **прежний** мир и годится как история,
  но не как источник решений. Токены — `dashboard/src/app/styles/tokens.css`.

## Evidence on Hand

- `.agents/docs/architecture/` — архитектура, журнал решений с альтернативами, стек,
  структура репозитория, Slice 0. Реальные документы проектирования, не реконструкция.
- `.agents/docs/product/comuki-fe-requirements.md` — **разделы, формы, состояния и
  матрица прав**. Самый конкретный источник по тому, что должно быть на экране.
- `.agents/docs/architecture/comuki-v1-scope-draft.md` — продуктовые решения v1,
  из которых выведены FE-требования.
- `dashboard/src/shared/ui/` — собственный кит на React Aria и CSS Modules
  (`DataTable` с виртуализацией, `SplitPane`, `Button`, `StatusBadge`,
  `ConfirmDialog`, `ComukiMark`); shadcn-остаток изолирован в `_legacy/` и
  вытесняется по доменам.
- `.agents/docs/design-system/` — прежний визуальный мир, историческая справка.
- `.agents/ROADMAP.md` и `.agents/STATE.md` — 9 фаз; сейчас milestone v1, phase 4 (Slice 0).

**Нет и выдумывать нельзя:** реальные прогоны, настоящие цифры стоимости и токенов,
клиенты, бенчмарки, скриншоты продакшена, отзывы. Все числа в мокапах — вымышленные
и должны помечаться как таковые.

## Product Principles

1. **Дежурный сканирует, а не читает.** Первый экран отвечает на «нужен ли я сейчас»;
   всё остальное — второй вопрос и второй уровень.
2. **Пульт, а не витрина.** Рядом с каждым наблюдением, из которого следует действие,
   должно быть само действие.
3. **Двести прогонов — норма, а не крайний случай.** Агрегация, группировка и фильтры —
   часть скелета, а не то, что добавят потом.
4. **Человек замыкает петлю.** Аппрувы, эскалации и правки управляющей плоскости всегда
   проходят через явное решение человека; интерфейс не имитирует автономию.
5. **Форма несёт смысл наравне с цветом.** Статус читается в монохроме — иконкой,
   позицией и весом, а не одним оттенком.

## Accessibility & Inclusion

Статус не передаётся одним цветом (дальтонизм) — требование зафиксировано в текущей
дизайн-системе и подтверждено принципом 5. Видимое состояние фокуса обязательно для
каждого интерактивного контрола. Конкретный уровень WCAG как обязательство не заявлен.

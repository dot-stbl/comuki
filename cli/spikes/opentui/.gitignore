node_modules/
*.log
.DS_Store

# Bun lockfiles (bun.lock, bun.lockb) are intentionally
# committed for spike-local reproducibility — see
# `bun install --frozen-lockfile` in the gate.

# Compiled standalone binary (the spike can rebuild it on demand
# via `bun run build:standalone` — see ADR-0002 §Packaging).
comuki-opentui-spike
comuki-opentui-spike.exe

# Generated measurement artefacts — recreated by `bun run scripts/*`.
# These are NOT checked in. The ADR references the *scripts* (which
# are deterministic) rather than the captured values.
evidence/*.txt
evidence/*.bin
evidence/*.log
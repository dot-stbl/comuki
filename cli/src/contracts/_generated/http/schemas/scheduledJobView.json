{
  "required": [
    "id",
    "projectId",
    "cronExpression",
    "profileKey",
    "briefJson",
    "runOnOnceAt",
    "enabled",
    "lastFiredAt",
    "nextFireAt",
    "createdAt",
    "updatedAt"
  ],
  "type": "object",
  "properties": {
    "id": { "type": "string", "description": "The job id.", "format": "uuid" },
    "projectId": {
      "type": "string",
      "description": "Owning project.",
      "format": "uuid"
    },
    "cronExpression": {
      "type": "string",
      "description": "5-field UTC cron expression."
    },
    "profileKey": {
      "type": "string",
      "description": "Profile the launched run will resolve."
    },
    "briefJson": { "type": "string", "description": "Worker brief payload." },
    "runOnOnceAt": {
      "type": ["null", "string"],
      "description": "Optional one-shot fire-at.",
      "format": "date-time"
    },
    "enabled": {
      "type": "boolean",
      "description": "Whether the dispatcher will fire it."
    },
    "lastFiredAt": {
      "type": ["null", "string"],
      "description": "Last fire stamp; null until the first dispatch.",
      "format": "date-time"
    },
    "nextFireAt": {
      "type": "string",
      "description": "Next computed fire stamp.",
      "format": "date-time"
    },
    "createdAt": { "type": "string", "format": "date-time" },
    "updatedAt": { "type": "string", "format": "date-time" }
  },
  "description": "Read projection of ScheduledJob for the REST surface.",
  "x-readme-ref-name": "ScheduledJobView"
}

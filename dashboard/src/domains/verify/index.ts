/**
 * The gate's surface: panels and the arithmetic behind them. There is no page
 * here any more — the screen that used to stand at `/verify` is folded into
 * Knowledge as its `gate` tab, and the assembly that mounted these panels now
 * lives in `domains/knowledge/ui/gate-tab.tsx`.
 */
export { VerifyProjectPanel } from "./ui/verify-project-panel"

/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@/shared/api/kubb-client"
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/shared/api/kubb-client"
import type {
  GetApiV1KeysQueryResponse,
  GetApiV1KeysQueryParams,
  GetApiV1Keys400,
} from "../types/GetApiV1Keys"

function getGetApiV1KeysUrl() {
  const res = { method: "GET", url: `/api/v1/keys` as const }
  return res
}

/**
 * @summary Lists API keys (issue #45 / F13 — read side of the identity admin).
 * Permission `identity:read`; returns a paged
 * `{ items, total }` envelope of ApiKeyView. The
 * plaintext token never appears in the response — only public-facing
 * fields (prefix, name, status, timestamps). Optional `userId`
 * narrows to a single user.
 * {@link /api/v1/keys}
 */
export async function getApiV1Keys(
  params?: GetApiV1KeysQueryParams,
  config: Partial<RequestConfig> & { client?: Client } = {}
) {
  const { client: request = fetch, ...requestConfig } = config

  const res = await request<
    GetApiV1KeysQueryResponse,
    ResponseErrorConfig<GetApiV1Keys400>,
    unknown
  >({
    method: "GET",
    url: getGetApiV1KeysUrl().url.toString(),
    params,
    ...requestConfig,
  })
  return res.data
}

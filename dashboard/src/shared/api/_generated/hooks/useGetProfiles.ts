/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/shared/api/kubb-client"
import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query"
import type { GetProfilesQueryResponse } from "../types/GetProfiles"
import { queryOptions, useQuery } from "@tanstack/react-query"
import { getProfiles } from "../clients/getProfiles"

export const getProfilesQueryKey = () => [{ url: "/profiles" }] as const

export type GetProfilesQueryKey = ReturnType<typeof getProfilesQueryKey>

export function getProfilesQueryOptions(
  config: Partial<RequestConfig> & { client?: Client } = {}
) {
  const queryKey = getProfilesQueryKey()
  return queryOptions<
    GetProfilesQueryResponse,
    ResponseErrorConfig<Error>,
    GetProfilesQueryResponse,
    typeof queryKey
  >({
    queryKey,
    queryFn: async ({ signal }) => {
      return getProfiles({ ...config, signal: config.signal ?? signal })
    },
  })
}

/**
 * {@link /profiles}
 */
export function useGetProfiles<
  TData = GetProfilesQueryResponse,
  TQueryData = GetProfilesQueryResponse,
  TQueryKey extends QueryKey = GetProfilesQueryKey,
>(
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetProfilesQueryResponse,
        ResponseErrorConfig<Error>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient }
    client?: Partial<RequestConfig> & { client?: Client }
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {}
  const { client: queryClient, ...resolvedOptions } = queryConfig
  const queryKey = resolvedOptions?.queryKey ?? getProfilesQueryKey()

  const query = useQuery(
    {
      ...getProfilesQueryOptions(config),
      ...resolvedOptions,
      queryKey,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<Error>> & {
    queryKey: TQueryKey
  }

  query.queryKey = queryKey as TQueryKey

  return query
}

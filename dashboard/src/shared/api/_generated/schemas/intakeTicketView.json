{
  "required": [
    "id",
    "projectId",
    "source",
    "externalId",
    "title",
    "url",
    "status",
    "runId",
    "createdAt"
  ],
  "type": "object",
  "properties": {
    "id": { "type": "string", "format": "uuid" },
    "projectId": { "type": "string", "format": "uuid" },
    "source": { "type": "string", "description": "Kebab-case provider key." },
    "externalId": { "type": "string" },
    "title": { "type": "string" },
    "url": { "type": "string" },
    "status": { "type": "string" },
    "runId": {
      "type": ["null", "string"],
      "description": "The launched run, when claimed.",
      "format": "uuid"
    },
    "createdAt": { "type": "string", "format": "date-time" }
  },
  "description": "Read-model of an intake ticket for the inbox and the API surfaces.",
  "x-readme-ref-name": "IntakeTicketView"
}

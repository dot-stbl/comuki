{
  "required": [
    "version",
    "minIdle",
    "maxConcurrent",
    "idleTtlSeconds",
    "approveRequired",
    "knowledgeEnabled",
    "verifyEnabled",
    "proxyEnabled",
    "softBudgetUsdMicros",
    "hardBudgetUsdMicros",
    "domainType",
    "customDomainTypesJson"
  ],
  "type": "object",
  "properties": {
    "version": {
      "pattern": "^-?(?:0|[1-9]\\d*)$",
      "type": ["integer", "string"],
      "format": "int32"
    },
    "minIdle": {
      "pattern": "^-?(?:0|[1-9]\\d*)$",
      "type": ["integer", "string"],
      "format": "int32"
    },
    "maxConcurrent": {
      "pattern": "^-?(?:0|[1-9]\\d*)$",
      "type": ["integer", "string"],
      "format": "int32"
    },
    "idleTtlSeconds": {
      "pattern": "^-?(?:0|[1-9]\\d*)$",
      "type": ["null", "integer", "string"],
      "format": "int32"
    },
    "approveRequired": { "type": "boolean" },
    "knowledgeEnabled": { "type": "boolean" },
    "verifyEnabled": { "type": "boolean" },
    "proxyEnabled": { "type": "boolean" },
    "softBudgetUsdMicros": {
      "pattern": "^-?(?:0|[1-9]\\d*)$",
      "type": ["null", "integer", "string"],
      "format": "int64"
    },
    "hardBudgetUsdMicros": {
      "pattern": "^-?(?:0|[1-9]\\d*)$",
      "type": ["null", "integer", "string"],
      "format": "int64"
    },
    "domainType": {
      "description": "Routing mode for user-facing domain types. Serialised as a string for readability.",
      "enum": ["Standard", "Custom", "Hybrid"],
      "x-readme-ref-name": "ProjectDomainType"
    },
    "customDomainTypesJson": {
      "type": ["null", "string"],
      "description": "Per-project JSON map of `domain-type → profile-key`."
    }
  },
  "description": "Wire body of PUT /api/v1/projects/{projectId}/settings.\r\nint UpdateSettingsRequest.Version is the version the client read — a stale version\r\nis refused with 409.",
  "x-readme-ref-name": "UpdateSettingsRequest"
}

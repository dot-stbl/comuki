/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import { z } from "zod/v4"

/**
 * @description What producing one message cost. Everything is optional — a user row\r\nhas no model and a journal row has no tokens — so a partial reading is\r\nexpressible without inventing zeroes.
 */
export const chatMessageMetaSchema = z
  .object({
    model: z
      .string()
      .describe(
        "Model id that produced the message (`glm-4.7`, …); null for rows no model wrote."
      )
      .nullish(),
    tokensIn: z
      .union([z.int(), z.string().regex(/^-?(?:0|[1-9]\d*)$/)])
      .describe("Prompt tokens consumed.")
      .nullish(),
    tokensOut: z
      .union([z.int(), z.string().regex(/^-?(?:0|[1-9]\d*)$/)])
      .describe("Completion tokens produced.")
      .nullish(),
    costMicros: z
      .union([z.int(), z.string().regex(/^-?(?:0|[1-9]\d*)$/)])
      .describe("Cost in USD micros (the unit the costs capability journals).")
      .nullish(),
    latencyMs: z
      .union([z.int(), z.string().regex(/^-?(?:0|[1-9]\d*)$/)])
      .describe("Wall-clock time to produce the message, in milliseconds.")
      .nullish(),
    stopReason: z
      .string()
      .describe("Why generation stopped (`stop`, `length`, `tool_use`, …).")
      .nullish(),
  })
  .describe(
    "What producing one message cost. Everything is optional — a user row\r\nhas no model and a journal row has no tokens — so a partial reading is\r\nexpressible without inventing zeroes."
  )

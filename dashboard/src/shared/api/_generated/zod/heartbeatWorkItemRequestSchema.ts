/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import { z } from "zod/v4"

/**
 * @description Heartbeat body: the generation the caller claimed this item under. The\r\n    endpoint binds this parameter as nullable — a pre-WS4/WS5 Translator that sends\r\n    no body at all still gets the documented 409 `work-item.not-owner` instead\r\n    of a 400 (see `WorkerEndpoints.HeartbeatAsync`).
 */
export const heartbeatWorkItemRequestSchema = z
  .object({
    generation: z
      .union([z.int(), z.string().regex(/^-?(?:0|[1-9]\d*)$/)])
      .describe(
        "A mismatch (the owning Run has since been cancelled/superseded) is treated as an ownership miss, same as today's owner/lease guard."
      ),
  })
  .describe(
    "Heartbeat body: the generation the caller claimed this item under. The\r\n    endpoint binds this parameter as nullable — a pre-WS4/WS5 Translator that sends\r\n    no body at all still gets the documented 409 `work-item.not-owner` instead\r\n    of a 400 (see `WorkerEndpoints.HeartbeatAsync`)."
  )

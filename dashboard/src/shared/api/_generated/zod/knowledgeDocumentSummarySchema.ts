/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import { z } from "zod/v4"

/**
 * @description Wire row of one knowledge source document (the library page) with its\r\nchunk aggregates. Money-free, vector-free — text metadata only.
 */
export const knowledgeDocumentSummarySchema = z
  .object({
    id: z.uuid().describe("Document id."),
    projectId: z.nullable(
      z.uuid().describe("Owning project; null = global corpus.")
    ),
    title: z.string().describe("Human-readable title."),
    source: z
      .string()
      .describe("Origin kind wire key: `git` | `upload` | `url`."),
    sourceRef: z
      .string()
      .describe("Origin pointer (git URL+ref, blob id, fetched URL)."),
    mimeType: z.string().describe("Detected MIME type of the original bytes."),
    chunkCount: z
      .union([z.int(), z.string().regex(/^-?(?:0|[1-9]\d*)$/)])
      .describe("Embedded chunks cut from the document."),
    tokenCount: z
      .union([z.int(), z.string().regex(/^-?(?:0|[1-9]\d*)$/)])
      .describe("Approximate token total across chunks."),
    createdAt: z.iso
      .datetime({ offset: true })
      .describe("When the document was registered."),
  })
  .describe(
    "Wire row of one knowledge source document (the library page) with its\r\nchunk aggregates. Money-free, vector-free — text metadata only."
  )

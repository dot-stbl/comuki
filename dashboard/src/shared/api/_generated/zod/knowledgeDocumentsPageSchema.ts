/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import { knowledgeDocumentSummarySchema } from "./knowledgeDocumentSummarySchema"
import { z } from "zod/v4"

/**
 * @description One page of documents plus the paging envelope (same shape as the runs page).
 */
export const knowledgeDocumentsPageSchema = z
  .object({
    get items() {
      return z
        .array(
          knowledgeDocumentSummarySchema.describe(
            "Wire row of one knowledge source document (the library page) with its\r\nchunk aggregates. Money-free, vector-free — text metadata only."
          )
        )
        .describe("Page rows.")
    },
    page: z
      .union([z.int(), z.string().regex(/^-?(?:0|[1-9]\d*)$/)])
      .describe("1-based page number."),
    pageSize: z
      .union([z.int(), z.string().regex(/^-?(?:0|[1-9]\d*)$/)])
      .describe("Rows per page."),
    total: z
      .union([z.int(), z.string().regex(/^-?(?:0|[1-9]\d*)$/)])
      .describe("Total documents matching the filter."),
  })
  .describe(
    "One page of documents plus the paging envelope (same shape as the runs page)."
  )

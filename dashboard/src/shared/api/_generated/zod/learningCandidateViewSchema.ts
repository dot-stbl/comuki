/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import { z } from "zod/v4"

/**
 * @description Read-model of one learning candidate for the approvals API and the\r\nlearning.suggest acknowledgement. Status is the wire key of\r\nLearningStatus (pending / approved / rejected).
 */
export const learningCandidateViewSchema = z
  .object({
    id: z.uuid().describe("Candidate id."),
    projectId: z
      .uuid()
      .describe("The project whose workers suggested the rule."),
    topic: z.string().describe("Short topic key (e.g. `build.dotnet`)."),
    observation: z
      .string()
      .describe("What the worker observed — the evidence."),
    proposedRule: z
      .string()
      .describe("The candidate rule text, stated as an imperative."),
    sourceRef: z
      .string()
      .describe("Where the signal came from (e.g. `worker:{id}`)."),
    repeatCount: z
      .union([z.int(), z.string().regex(/^-?(?:0|[1-9]\d*)$/)])
      .describe("How many times the same signal repeated."),
    status: z.string().describe("Wire key of the review state."),
    decisionReason: z.nullable(
      z.string().describe("Why the human rejected it; null unless rejected.")
    ),
    createdAt: z.iso
      .datetime({ offset: true })
      .describe("When the candidate was first seen."),
    decidedAt: z.nullable(
      z.iso
        .datetime({ offset: true })
        .describe("When the human decided; null while pending.")
    ),
  })
  .describe(
    "Read-model of one learning candidate for the approvals API and the\r\nlearning.suggest acknowledgement. Status is the wire key of\r\nLearningStatus (pending / approved / rejected)."
  )

/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import { z } from "zod/v4"

/**
 * @description One limit\'s usage against its tier cap. The `current` value comes\r\nfrom the matching ILimitUsageProvider registered in DI;\r\nwhen no provider is registered the view reports `0` rather than\r\nrefusing to answer, because the gate is the source of truth for\r\nenforcement and the view is read-only.
 */
export const limitUsageViewSchema = z
  .object({
    key: z
      .string()
      .describe(
        "The closed-vocabulary limit key (dot.case; the same string the gate attribute carries)."
      ),
    current: z
      .union([z.int(), z.string().regex(/^-?(?:0|[1-9]\d*)$/)])
      .describe(
        "Currently-observed usage (best-effort from the matching provider, or 0)."
      ),
    cap: z
      .union([z.int(), z.string().regex(/^-?(?:0|[1-9]\d*)$/)])
      .describe("Effective numeric cap at the current tier."),
  })
  .describe(
    "One limit's usage against its tier cap. The `current` value comes\r\nfrom the matching ILimitUsageProvider registered in DI;\r\nwhen no provider is registered the view reports `0` rather than\r\nrefusing to answer, because the gate is the source of truth for\r\nenforcement and the view is read-only."
  )

/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import { planEdgeSchema } from "./planEdgeSchema"
import { planNodeSchema } from "./planNodeSchema"
import { z } from "zod/v4"

/**
 * @description The decomposition DAG — the approve card. Nodes and edges are the\r\ncanonical Plan shapes, so a part and a plan payload\r\nnever drift apart.
 */
export const messagePartPlanPartSchema = z
  .object({
    kind: z.optional(z.enum(["plan"])),
    get nodes() {
      return z
        .array(
          planNodeSchema.describe(
            "One step of a plan, executed by one worker with one profile."
          )
        )
        .describe("Plan steps.")
    },
    get edges() {
      return z
        .array(
          planEdgeSchema.describe(
            "A dependency: To starts after From finishes."
          )
        )
        .describe("Ordering dependencies between steps.")
    },
  })
  .describe(
    "The decomposition DAG — the approve card. Nodes and edges are the\r\ncanonical Plan shapes, so a part and a plan payload\r\nnever drift apart."
  )

/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import { z } from "zod/v4"

/**
 * @description Wire row of one virtual key — fingerprinted; no token material.
 */
export const proxyKeyViewSchema = z
  .object({
    id: z
      .string()
      .describe("SHA-256 fingerprint of the token — the id revoke addresses."),
    prefix: z.string().describe("Display prefix of the token."),
    projectId: z.uuid().describe("Project spend attribution."),
    provider: z
      .string()
      .describe("Upstream cluster id: `openai` / `anthropic` / `custom`."),
    baseUrl: z.string().describe("Upstream root URL."),
    defaultModel: z.nullable(
      z
        .string()
        .describe(
          "Default model when the caller's body omits one; null = none."
        )
    ),
    allowedModels: z
      .array(z.string())
      .describe("Model allow-list; empty = every model permitted."),
    budgetUsd: z.nullable(
      z
        .union([z.number(), z.string().regex(/^-?(?:0|[1-9]\d*)(?:\.\d+)?$/)])
        .describe("Optional monthly USD cap; null = unlimited.")
    ),
    expiresAt: z.nullable(
      z.iso
        .datetime({ offset: true })
        .describe("Optional UTC expiry; null = never.")
    ),
  })
  .describe("Wire row of one virtual key — fingerprinted; no token material.")

/**
 * The kit's combobox field — a labelled text input that suggests from a closed
 * list while still accepting what is typed.
 *
 * The folder is the unit, the way `select/` and `search-field/` are: domains
 * import from `@/shared/ui` and nothing reaches past this file.
 */
export {
  ComboboxField,
  type ComboboxFieldOption,
  type ComboboxFieldProps,
} from "./combobox-field"

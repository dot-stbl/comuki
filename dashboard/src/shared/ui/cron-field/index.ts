/**
 * The kit's cron field — a schedule expression with the reading of it underneath.
 *
 * The folder is the unit, the way `select/` and `search-field/` are: domains
 * import from `@/shared/ui` and nothing reaches past this file.
 */
export { CronField, type CronFieldProps } from "./cron-field"

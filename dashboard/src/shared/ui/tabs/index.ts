/**
 * The kit's tabs — a thin styled wrapper over `react-aria-components`'s
 * `Tabs`, `TabList`, `Tab`, and `TabPanel`. The migration of the three
 * pages that currently import directly from `react-aria-components` is
 * a follow-up; this primitive is the styled version they will move to.
 */
export {
  Tabs,
  TabList,
  Tab,
  TabPanel,
  type TabsProps,
  type TabListProps,
  type TabProps,
  type TabPanelProps,
} from "./tabs"

apiVersion: 1

providers:
  - name: comuki
    orgId: 1
    folder: Comuki
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards

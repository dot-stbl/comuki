apiVersion: v1
kind: Service
metadata:
  name: comuki
  namespace: comuki
  labels:
    app.kubernetes.io/name: comuki
    app.kubernetes.io/component: host
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app.kubernetes.io/name: comuki
    app.kubernetes.io/component: host

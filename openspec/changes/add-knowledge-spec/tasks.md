## 1. Spec backfill (existing behavior)

Each task below verifies one requirement of the `knowledge` capability
against the named source file(s). All checked — this is a docs-only
change and the behavior was already in production.

- [x] 1.1 `Source document aggregate` — verify `Comuki.Modules.Knowledge.Domain/SourceDocument.cs`, `SourceKind.cs`, `SourceKindKeys.cs` (required fields, wire keys, factory validation, `Create` throws on empty title / sourceRef / mimeType)
- [x] 1.2 `Paragraph-aware chunking` — verify `Comuki.Modules.Knowledge.Infrastructure/Chunking/Chunker.cs`, `Configuration/KnowledgeIngestOptions.cs`, `tests/unit/Comuki.Modules.Knowledge.Unit/ChunkerShould.cs` (split on `\n\n`, `targetTokens` range `[64, 8192]` default 500, `Math.Ceiling(words / 0.75)`, over-target paragraph stays whole, empty text yields zero chunks)
- [x] 1.3 `Ingestion writes chunks and embeddings (independent rows per call)` — verify `Infrastructure/Persistence/PgKnowledgeIngestor.cs`, `Domain/MemoryEmbedding.cs`, `Infrastructure/Persistence/Configurations/SourceDocumentConfiguration.cs`, `tests/unit/.../PgKnowledgeIngestorShould.cs` (every `IngestAsync` inserts a NEW row, no `WHERE` lookup, no DELETE of prior chunks, single index `ix_source_documents_project_created`, zero-chunk text commits the document with `ChunksWritten = 0`, embedding-count mismatch throws, pgvector column absent → vector UPDATE skipped)
- [x] 1.4 `Project and global write scope` — verify `PgKnowledgeIngestor.IsProjectWritable`, `tests/unit/.../PgKnowledgeIngestorShould.cs` (`RefuseIngestForProjectOutsideScopeAsync`, `RefuseIngestForGlobalCorpusWhenRestrictedAsync`, `AllowUnrestrictedCallerAnyProjectAsync`) (unrestricted writes any project or global; restricted writes only assigned projects, never global; `ProviderForbiddenException` code `knowledge.project_out_of_scope`, loud 403, no row persisted)
- [x] 1.5 `pgvector cosine similarity search` — verify `Infrastructure/Persistence/PgKnowledgeSearcher.cs`, `Persistence/Stores/EmbeddingSql.cs`, `tests/unit/.../PgKnowledgeSearcherShould.cs`, `tests/integration/.../PgKnowledgeSearcherShould.cs` (raw-SQL cosine-distance query ordered by similarity desc, `topK` `[1, 1000]`, `minSimilarity` `[0.0, 1.0]`, blank query throws before DB touch, pgvector absent → empty list)
- [x] 1.6 `Project and global read scope` — verify `PgKnowledgeSearcher.SearchAsync`'s scope clause, `EmbeddingSql.CosineSearchSql`'s `Visibility` + `Narrowing` clauses, `tests/integration/.../PgKnowledgeSearcherShould.cs` (`ExcludeAnotherProjectsChunkFromScopedSearchAsync`) (out-of-scope `projectId` → empty list, NOT 403/404; unrestricted any project; restricted sees global + own projects, never another's)
- [x] 1.7 `Embedding provider selection` — verify `Configuration/KnowledgeEmbeddingOptions.cs`, `KnowledgeInfrastructureExtensions.AddKnowledgeEmbeddingClient`, `Embeddings/NoopEmbeddingClient.cs`, `tests/unit/.../KnowledgeOptionsShould.cs`, `NoopEmbeddingClientShould.cs`, `OpenAIEmbeddingClientShould.cs` (`noop` default noop no key, `openai` throws on missing `ApiKeyEnvRef`, `voyage` throws `NotSupportedException` reserved, default `Dimensions = 1536` matches `EmbeddingSql.Dimensions`, `BatchSize` `[1, 256]` default 32)
- [x] 1.8 `Document listing` — verify `Application/Documents/IKnowledgeDocumentReader.cs`, `Application/Documents/KnowledgeDocumentsPage.cs`, `Infrastructure/Persistence/PgKnowledgeDocumentReader.cs`, `tests/unit/.../KnowledgeDocumentsPagingShould.cs` (newest-first `CreatedAt DESC` + `Id DESC`, chunk count + token total aggregates, subject-scope filter on both items and total, `page` ≥ 1, `pageSize` clamped `[1, 100]`)
- [x] 1.9 `REST surface` — verify `Comuki.Host/Knowledge/KnowledgeModuleEndpoints.cs`, `Comuki.Host/ApiRoutes.cs`, `tests/integration/.../KnowledgeShould.cs` (three routes under `/api/v1/knowledge`: `POST /ingest` returns 200 `{ sourceDocumentId, chunksWritten }`, `GET /documents` paged list, `GET /search` with `q` required and 400 codes `knowledge.query_required` / `knowledge.top_k_out_of_range` / `knowledge.min_similarity_out_of_range`)
- [x] 1.10 `MCP tools` — verify `Comuki.Host/Mcp/McpToolCatalog.cs` (tool names `knowledge.search`, `knowledge.ingest` — NOT `search_knowledge`), `McpToolHandlers.cs` (`KnowledgeSearchAsync`, `KnowledgeIngestAsync`, `McpKnowledgeSearch`), `McpToolPermissionMap.cs`, `McpServer.cs` (dispatch switch on lines 140–141) (worker caller never accepts client `projectId`, no-lease worker → `IsError=true` text result, subject may pass or omit `projectId`, missing catalog entry / unauthenticated subject = deny)
- [x] 1.11 `Permissions` — verify `Comuki.Modules.Identity.Domain/Permissions/Permissions.cs`, `Roles/RoleMatrix.cs` (`knowledge:read` granted to `PlatformAdmin`, `Operator`, `ProjectAdmin`, `Member`, `Viewer` only — `Approver` excluded; `knowledge:write` granted to `PlatformAdmin`, `Operator`, `ProjectAdmin` only — `Member` and `Approver` excluded; `knowledge:admin` granted to `PlatformAdmin` only and not checked by any surface today. The grants are an explicit role list, NOT a seniority threshold, because `Approver` (seniority 40) sits between `Member` (30) and `ProjectAdmin` (60) yet holds neither permission.)
- [x] 1.12 `Doc worker heartbeat` — verify `Infrastructure/Hosted/KnowledgeIngestBackgroundService.cs`, `tests/unit/.../KnowledgeIngestBackgroundServiceShould.cs` (interval `Knowledge:Ingest:PollIntervalSeconds` default 60 range `[1, 86400]` floored to 1s, start/stop logs, tick exception caught+logged and does not stop loop, today's tick body is heartbeat only with no `KnowledgeSource` table yet)
- [x] 1.13 `Persistence layout` — verify `Infrastructure/Persistence/KnowledgeDatabase.cs` (schema `knowledge`, tables `source_documents` + `memory_embeddings` + `__ef_migrations_history`), `Infrastructure/Persistence/Configurations/MemoryEmbeddingConfiguration.cs` (unique index `(source_document_id, chunk_index)` named `ix_memory_embeddings_source_chunk`), `Migrations/` (embedding column conditional on pgvector, raw-SQL managed)

## 2. README sync into the main capability tree

- [ ] 2.1 Sync the new `knowledge` capability into `openspec/specs/knowledge/spec.md` and add a row to `openspec/README.md`'s capability map table — NOT in this change. This task is intentionally unchecked and is owned by the apply/archive step (which runs once this change is reviewed and merged), not by the spec-authoring change. Doing it here would touch files outside `openspec/changes/add-knowledge-spec/**`, which is out of scope for this PR.

## 3. New capability contract for context-fabric (not yet implemented)

Each task below is the implementation work for one of the 3 planned
requirements (14–16 in `spec.md`). All deferred to a follow-up change
— NOT this one.

- [ ] 3.1 Implement `Stable source key and content hash` — add a stable source key column on `SourceDocument` distinct from `SourceRef`, add an immutable content hash column computed at ingest time, add a unique index on `(project_id, source_key)`, change `IngestAsync`'s lookup to find an existing row by `(projectId, source_key)` and either reuse the row (hash match) or create a new revision (hash mismatch). Deferred — follow-up change, not this one.
- [ ] 3.2 Implement `Revision and generation tracking on reingest` — when a reingest under the same source key produces a new content hash, create a new revision, mark it the active generation, mark prior chunks stale-but-auditable, and ensure `SearchAsync` only returns chunks from the currently active generation. Deferred — follow-up change, not this one.
- [ ] 3.3 Implement `Provenance on search hits` — extend `KnowledgeSearchHit` to carry `sourceKey`, `revisionId`, and `generation`, so Context Pack citations can resolve provenance end-to-end. Deferred — follow-up change, not this one.

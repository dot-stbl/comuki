## 1. Spec backfill
- [x] Proposal for chat + memory capabilities
- [x] Delta specs `chat` + `memory`
- [x] Sync deltas into `openspec/specs/{chat,memory}/spec.md`
- [x] Update `openspec/README.md` capability map
- [x] Mark `add-chat-memory` tasks against landed code (partial complete)

## 2. Verification
- [x] `openspec validate --specs` (or change validate) after sync
- [x] Spot-check routes vs `ApiRoutes` / controllers

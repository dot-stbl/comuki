## Design (docs backfill)

Documents landed Wave 6 platform seams:
- Kubernetes batch/v1 Job provider (UUID7-safe job names)
- Ambient SubjectScope + EF query filters (404 object axis)
- Shared Filtering DSL + runs list API
- OTel installer opt-in + Grafana as-code under deploy/

No runtime changes in this change.

<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Comuki.Shared.Bootstrap</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Configuration" />
    <PackageReference Include="Microsoft.Extensions.Configuration.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.Hosting" />
    <PackageReference Include="Microsoft.Extensions.Logging.Console" />
    <PackageReference Include="Tomlyn" />
  </ItemGroup>

  <ItemGroup>
    <!-- Editions reference added for worker feature gating
         (ComukiWorkerRegistry reads IEdition; see Workers/WorkerFeatureGate.cs).
         Acrylic: Editions depends on Kernel only; Bootstrap depending on Editions
         keeps the dependency graph one-directional. -->
    <ProjectReference Include="..\Comuki.Shared.Editions\Comuki.Shared.Editions.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Comuki.Shared.Bootstrap.Unit" />
  </ItemGroup>

</Project>
